const title="Mosaic Dream";let seed,dx,dy,maxCanvas,mainCanvas,borderW,palette,size,r,numb,theme,x,myColors;setTitle(title);let stroke_,strokeColor,col=pal;function start(){seed=$fx.getParam("seed_id"),randomSeed(seed),palette=$fx.getParam("palette_id"),setColors(),x=[.102,.168,.2,.334,.5],theme=$fx.getParam("theme_id"),"very small"==theme?size=floor(maxCanvas*x[0]):"small"==theme?size=floor(maxCanvas*x[1]):"medium"==theme?size=floor(maxCanvas*x[2]):"big"==theme?size=floor(maxCanvas*x[3]):"very big"==theme&&(size=floor(maxCanvas*x[4])),r=floor(maxCanvas*$fx.getParam("space_id")),stroke_=maxCanvas*$fx.getParam("strokeW_id"),strokeColor="#"+$fx.getRawParam("strokeColor_id")}function setup(){dx=windowWidth,dy=windowWidth,maxCanvas=min(windowWidth,windowHeight),start(),mainCanvas=createCanvas(dx,dy),height>windowHeight&&(dx=windowHeight,dy=windowHeight,resizeCanvas(dx,dy)),pixelDensity(12),rectMode(CENTER),background("#222"),noLoop(),strokeWeight(stroke_),stroke(strokeColor)}function createSquare(e,a,o,t){let r=e/a,s=a;for(let a=0;a<r;a++)push(),"(all random)"==palette?fill(`#${random(random(pal))}`):fill(`#${random(myColors)}`),square(o,t,e),pop(),e-=s}function draw(){for(let e=floor(-size/2);e<dx;e+=size)for(let a=floor(-size/2);a<dy;a+=size)createSquare(size,r,e,a);fxpreview()}