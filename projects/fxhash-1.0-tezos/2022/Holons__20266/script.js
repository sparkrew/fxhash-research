function e(e){return e&&e.__esModule?e.default:e}const n=(e,n,r)=>{const t=e.createShader(r);if(e.shaderSource(t,n),e.compileShader(t),!e.getShaderParameter(t,e.COMPILE_STATUS)){const n=e.getShaderInfoLog(t);throw new Error(n)}return t},r=(e,r,t,o)=>{const i=n(e,r,e.VERTEX_SHADER),a=n(e,t,e.FRAGMENT_SHADER),s=e.createProgram();if(e.attachShader(s,i),e.attachShader(s,a),o){const n=[];for(let e in o)n.push(o[e].varying);e.transformFeedbackVaryings(s,n,e.SEPARATE_ATTRIBS)}if(e.linkProgram(s),!e.getProgramParameter(s,e.LINK_STATUS)){const n=e.getProgramInfoLog(s);throw new Error(n)}return s},t=(e,n)=>{const r=e.getProgramParameter(n,e.ACTIVE_UNIFORMS),t={};for(let o=0;o<r;o++){const{name:r,type:i}=e.getActiveUniform(n,o);t[r]={location:e.getUniformLocation(n,r),type:i}}return t},o=(e,n,r=e.STATIC_DRAW,t=e.ARRAY_BUFFER)=>{const o=e.createBuffer();return e.bindBuffer(t,o),e.bufferData(t,n,r),o},i=(e,n,r)=>{if(!r)return null;const t=e.createVertexArray(),i=Object.keys(r);e.bindVertexArray(t);for(let t=0;t<i.length;t++){let{size:a=1,type:s=e.FLOAT,normalize:c=!1,stride:l=0,offset:f=0,buffer:d,data:u,usage:v=e.STATIC_DRAW,buffertype:p=e.ARRAY_BUFFER}=r[i[t]];u&&!d&&(d=o(e,u,v,p)),e.bindAttribLocation(n,t,i[t]),e.enableVertexAttribArray(t),e.bindBuffer(e.ARRAY_BUFFER,d),e.vertexAttribPointer(t,a,s,c,l,f)}return t},a=(e,n,r)=>{if(!r)return null;const t=[e.createVertexArray(),e.createVertexArray()],i=[e.createTransformFeedback(),e.createTransformFeedback()],a=[],s=Object.keys(r);return t.forEach(((t,c)=>{e.bindVertexArray(t);const l=[],f={};for(let t=0;t<s.length;t++){let{size:i=1,type:a=e.FLOAT,normalize:c=!1,stride:d=0,offset:u=0,buffer:v,data:p,usage:m=e.STATIC_DRAW,buffertype:x=e.ARRAY_BUFFER}=r[s[t]];p&&!v&&(v=o(e,p,m,x)),f[s[t]]={i:t,buffer:v},e.bindAttribLocation(n,t,s[t]),e.enableVertexAttribArray(t),e.bindBuffer(e.ARRAY_BUFFER,v),e.vertexAttribPointer(t,i,a,c,d,u),l.push(v)}e.bindBuffer(e.ARRAY_BUFFER,null),e.bindTransformFeedback(e.TRANSFORM_FEEDBACK,i[c]),l.forEach(((n,r)=>{e.bindBufferBase(e.TRANSFORM_FEEDBACK_BUFFER,r,n)})),e.bindVertexArray(null),a.push(f)})),{vaos:t,tfbs:i,tfbBuffers:a}},s=(e,{vert:n,frag:o,attributes:s,transformFeedbacks:c,count:l,offset:f=0,mode:d=e.TRIANGLES}={})=>{const u=r(e,n,o,c),v=t(e,u),p=i(e,u,s),m=a(e,u,c);let x=0;return{uniforms:v,bindUniform(n,r){if(void 0===r)return;if(!v[n])return;const{type:t,location:o}=v[n];switch(t){case 5126:return r instanceof Array?e.uniform1fv(o,r):("number"==typeof r&&e.uniform1f(o,r),null);case 35664:return e.uniform2fv(o,r);case 35665:return e.uniform3fv(o,r);case 35666:return e.uniform4fv(o,r);case 35670:case 5124:case 35678:case 35680:return r.length?e.uniform1iv(o,r):e.uniform1i(o,r);case 35671:case 35667:return e.uniform2iv(o,r);case 35672:case 35668:return e.uniform3iv(o,r);case 35673:case 35669:return e.uniform4iv(o,r);case 35674:return e.uniformMatrix2fv(o,!1,r);case 35675:return e.uniformMatrix3fv(o,!1,r);case 35676:if(null===r)return;return e.uniformMatrix4fv(o,!1,r)}},bindVAOs(){e.useProgram(u),p&&e.bindVertexArray(p)},bindTransformFeedbacks(){if(!m)return;const n=(x+1)%2,r=m.vaos[x],t=m.tfbs[n],o=m.tfbBuffers[n];this.readTFB=r,this.writeTFB=m.vaos[n],e.bindVertexArray(r),e.bindTransformFeedback(e.TRANSFORM_FEEDBACK,t);for(let n in o){const r=o[n];e.bindBufferBase(e.TRANSFORM_FEEDBACK_BUFFER,r.i,r.buffer)}x=n},render(n,r){e.viewport(0,0,n||e.canvas.width,r||e.canvas.height),m&&e.beginTransformFeedback(d),e.drawArrays(d,f,l),m&&e.endTransformFeedback()}}},c=(e,{width:n,height:r,tiling:t=e.CLAMP_TO_EDGE,type:o=e.UNSIGNED_BYTE,format:i=e.RGBA,internalFormat:a=e.RGBA,data:s=null,singular:c=!1,minFilter:l=e.LINEAR,magFilter:f=l,mips:d=!1}={})=>{const u=({w:c=n,h:u=r}={})=>{const v=e.createTexture();e.bindTexture(e.TEXTURE_2D,v),e.texImage2D(e.TEXTURE_2D,0,a,c,u,0,i,o,s),e.texParameteri(e.TEXTURE_2D,e.TEXTURE_MIN_FILTER,l),e.texParameteri(e.TEXTURE_2D,e.TEXTURE_MAG_FILTER,f),e.texParameteri(e.TEXTURE_2D,e.TEXTURE_WRAP_S,t),e.texParameteri(e.TEXTURE_2D,e.TEXTURE_WRAP_T,t);const p=e.createFramebuffer();return e.bindFramebuffer(e.FRAMEBUFFER,p),e.framebufferTexture2D(e.FRAMEBUFFER,e.COLOR_ATTACHMENT0,e.TEXTURE_2D,v,0),d&&e.generateMipmap(e.TEXTURE_2D),{fb:p,targetTexture:v}};return{width:n,height:r,read:u({w:n,h:r}),write:c?null:u({w:n,h:r}),resize(e,n){this.width=e,this.height=n,this.read=u({w:e,h:n}),this.write=u({w:e,h:n})},swap(){if(c)return;const e=this.read;this.read=this.write,this.write=e}}};let l=parseInt(2200),f=parseInt(3e3),d=l/f,u=fxrand(),v=0;v=u<.35?0:u<.45?1:u<.7?2:u<.9?3:4;let p=parseInt(4*Math.pow(fxrand(),1))+1,m=Math.pow(parseInt(7*fxrand())+4,2),x=[],_=p*p;1==v&&(_=2);let y=0;for(let e=0;e<_;e++)x[e]=fxrand()<.1?0:1,1==_&&(x[e]=1),1==x[e]&&y++;let b=0,g=fxrand();b=g<.14?0:g<.28?1:g<.34?2:g<.57?3:g<.61?4:g<.7?5:g<.74?6:g<.83?7:g<.97?8:9;let h=0,T=fxrand();h=T<.12?0:T<.22?1:T<.25?2:T<.42?3:T<.58?4:T<.64?5:T<.81?6:T<.87?7:T<.9?8:9;let D=0,E=fxrand();1==p&&(E=.95*fxrand()),D=E<.55?0:1,7!=h&&9!=h&&4!=b||(D=1);let z=0;z=fxrand()<.98?0:1;let w,F,R,U,P,A,S,I,M=Math.min(parseInt(4*Math.pow(fxrand(),2)),3),B=fxrand();B<.24?(P=0,w=[.08,.08,.08],F=[.5,.5,.5],R=[1,.2,.1],U=[.48,.77,.97]):B<.39?(P=1,w=[.3,.5,1],F=[1,.5,.05],R=[1,.5,.2],U=[1,.2,.3]):B<.83?(P=2,w=[1,.1,0],F=[.3,.8,.9],R=[.3,.8,.9],U=[1,.5,.1]):B<.93?(P=3,w=[.1,.14,.28],F=[.45,.6,.83],R=[.97,.73,.38],U=[.25,.14,.08]):B<.95?(P=4,w=[1,1,.2],F=[1,.8,.3],R=[.3,.5,.8],U=[.4,.8,.5]):(P=5,w=[1,1,.2],F=[1,.8,.3],R=[.3,.5,.8],U=[.4,.8,.5]);let C,O=fxrand();O<.17?(C=0,A=[.01,.015,.1],S=[.01,.5,.8],I=[.5,1,.3]):O<.3?(C=1,A=[.08,.02,.04],S=[.6,.1,.2],I=[1,1,.1]):O<.47?(C=2,A=[.015,.035,.04],S=[.6,.1,.2],I=[1,.2,.1]):O<.55?(C=3,A=[.05,.01,.02],S=[.2,.05,.05],I=[.6,1,1]):O<.59?(C=4,A=[.025,.01,.08],S=[.05,.6,.15],I=[1,1,.5]):O<.95?(C=5,A=[.01,.01,.01],S=[.25,.25,.25],I=[1,1,1]):(C=6,A=[1,1,1],S=[0,0,0],I=[0,0,0]),0!=D&&6!=C||(M=0,z=0),1==p&&(z=0);let L=0,G=1,V=fxrand();V<.6?(L=0,G=1):V<.85?(L=1,G=.75):(L=2,G=.5),1==D&&(L=0,G=1);let X=["Stacks","Twins","Anomalies","Surrounding Anomalies","Surrounding Anomalies"],N=["Bursts","Flames","Facets","Swarms","Flows","Molten","Combustion","Vibrating Nodes","Membranes","Unknown Paths"],q=["Burst","Flame","Duality","Swarm","Organism","Facet","Radiate","Twinkle","Flow","Unknown Path"],k=["Broad","Tight"],Y=["Luscious","Solar","Plasma","Frost","Voltage","Light","Charcoal","Significant","Mereological","Integral","Fundamental","Autopoiesis","Verisimilitude"],H=["Absent","Present"],j=["Absent","Interlace","Radial","Broad Radial"],K=["Full","Mid","Mild"],W=C;0==D&&(W=7+P),window.$fxhashFeatures={Construct:X[v],Transcendent:N[h],Inclusion:q[b],Spectrum:k[D],Coloring:Y[W],Enclosures:H[z],Screen:j[M],Intensity:K[L],Elements:y},console.log("obstacle style (Construct) = "+v+" "+X[v]),console.log("exterior (Transcendent) = "+h+" "+N[h]),console.log("interior (Inclusion) = "+b+" "+q[b]),console.log("color (Spectrum) = "+D+" "+k[D]),console.log("palette (Coloring) = "+W+" "+Y[W]),console.log("shell (Enclosure) = "+z+" "+H[z]),console.log("screen (Screen) = "+M+" "+j[M]),console.log("vibrancy (Intensity) = "+L+" "+K[L]),console.log("obstacles (Elements) = "+y),console.log("num forces = "+m);const Z={dpr:2,width:l,height:f,aspect:d,mapSize:512,particles:5e5,mapScale:1e6/Math.pow(512,2)*2*16,numForces:m,obstacleSize:p,obstacleType:v,obstacleRands:x,lightDir0:[0,-1,.4],lightDir1:[0,1,.15],interiorType:b,exteriorType:h,shellType:z,colorMode:D,screenMode:M,rf0:fxrand(),rf1:fxrand(),paletteId:C,cd0:A,cd1:S,cd2:I,velColorId:P,c0:w,c1:F,c2:R,c3:U,featurePalette:W,saturation:G};var $;$="#version 300 es\nprecision highp float;\n#define GLSLIFY 1\nin vec2 position;\nin vec2 uv;\nout vec2 v_uv;\n\nvoid main() {\n    v_uv = uv;\n    gl_Position = vec4( position, 0, 1 );\n}";const J=new Float32Array([-1,-1,3,-1,-1,3]),Q=new Float32Array([0,0,2,0,0,2]),ee=1/Z.mapSize,ne=n=>{const r={};return r.renderer=s(n,{vert:e($),frag:e("#version 300 es\nprecision highp float;\n#define GLSLIFY 1\nin vec2 v_uv;\nout vec4 color;\n\nuniform vec2 u_inverseResolution;\nuniform vec2 u_direction;\nuniform vec2 u_resolution;\nuniform sampler2D u_buffer;\n\nvec4 blur(sampler2D image, vec2 uv, vec2 inverseResolution, vec2 direction) {\n  vec4 color = vec4(0.0);\n  vec2 off1 = vec2(1.411764705882353) * direction;\n  vec2 off2 = vec2(3.2941176470588234) * direction;\n  vec2 off3 = vec2(5.176470588235294) * direction;\n  color += texture(image, uv) * 0.1964825501511404;\n  color += texture(image, uv + (off1 * inverseResolution)) * 0.2969069646728344;\n  color += texture(image, uv - (off1 * inverseResolution)) * 0.2969069646728344;\n  color += texture(image, uv + (off2 * inverseResolution)) * 0.09447039785044732;\n  color += texture(image, uv - (off2 * inverseResolution)) * 0.09447039785044732;\n  color += texture(image, uv + (off3 * inverseResolution)) * 0.010381362401148057;\n  color += texture(image, uv - (off3 * inverseResolution)) * 0.010381362401148057;\n  return color;\n}\n/*\nvec4 blur(sampler2D image, vec2 uv, vec2 inverseResolution, vec2 direction) {\n    vec4 color = vec4(0.0);\n    vec2 off1 = vec2(1.3846153846) * direction;\n    vec2 off2 = vec2(3.2307692308) * direction;\n    color += texture(image, uv) * 0.2270270270;\n    color += texture(image, uv + (off1 * inverseResolution)) * 0.3162162162;\n    color += texture(image, uv - (off1 * inverseResolution)) * 0.3162162162;\n    color += texture(image, uv + (off2 * inverseResolution)) * 0.0702702703;\n    color += texture(image, uv - (off2 * inverseResolution)) * 0.0702702703;\n    return color;\n}*/\n/*\nvec4 blur(sampler2D image, vec2 uv, vec2 inverseResolution, vec2 direction) {\n    vec4 color = vec4(0.0);\n    vec2 off1 = vec2(1.3333333333333333) * direction;\n    color += texture(image, uv) * 0.29411764705882354;\n    color += texture(image, uv + (off1 * inverseResolution)) * 0.35294117647058826;\n    color += texture(image, uv - (off1 * inverseResolution)) * 0.35294117647058826;\n    return color;\n}*/\n\nvoid main() {\n  vec2 uv = gl_FragCoord.xy / u_resolution;\n  color = blur(u_buffer, uv, u_inverseResolution, u_direction);\n}"),attributes:{position:{data:J,size:2},uv:{data:Q,size:2}},count:3}),r.fb=c(n,{width:Z.mapSize,height:Z.mapSize,tiling:n.REPEAT,type:Z.fbType,internalFormat:Z.fbFormat}),r},re=(e,n,r,t,o,i,a)=>{r.renderer.bindVAOs(),e.bindFramebuffer(e.FRAMEBUFFER,r.fb.write.fb),e.activeTexture(e.TEXTURE0),e.bindTexture(e.TEXTURE_2D,t),r.renderer.bindUniform("u_buffer",0),r.renderer.bindUniform("u_direction",o),r.renderer.bindUniform("u_inverseResolution",[ee,ee]),r.renderer.bindUniform("u_resolution",[i,a]),r.renderer.render(i,a),r.fb.swap()},te=(e,n,r,t,o,i)=>(re(e,0,t,r,[1,0],o,i),re(e,0,t,t.fb.read.targetTexture,[0,1],o,i),t.fb.read.targetTexture),oe={},ie=n=>{const r=Z.particles;oe.renderer=s(n,{vert:e("#version 300 es\n#define GLSLIFY 1\n\nin vec2 a_position;\nin vec2 a_velocity;\n\nuniform vec2 u_screensize;\n\nout vec2 state;\n\nvoid main() {\n    gl_PointSize = 1.;\n    state = a_position / u_screensize;\n    gl_Position = vec4( state * 2. - 1., 0., 1.);\n}"),frag:e("#version 300 es\nprecision highp float;\n#define GLSLIFY 1\n\nin vec2 state;\n\nuniform float u_scale;\n\nout vec4 color;\n\nvoid main() {\n  color = vec4( fract( state + 0.25 ) - 0.25, fract( state - 0.25 ) + 0.25 ) * u_scale;\n}"),count:r,mode:n.POINTS}),oe.fb=c(n,{width:Z.mapSize,height:Z.mapSize,tiling:n.REPEAT,type:Z.fbType,internalFormat:Z.fbFormat,singular:!0}),oe.blur=ne(n)};const ae=(e,n)=>{oe.renderer.bindVAOs(),e.bindVertexArray(Ue.renderer.readTFB),e.bindFramebuffer(e.FRAMEBUFFER,oe.fb.read.fb),oe.renderer.bindUniform("u_screensize",[Z.height,Z.height]),oe.renderer.bindUniform("u_scale",Z.mapScale),e.clear(e.DEPTH_BUFFER_BIT),e.clear(e.DEPTH_BUFFER_BIT|e.COLOR_BUFFER_BIT),e.enable(e.BLEND),e.blendEquationSeparate(e.FUNC_ADD,e.FUNC_ADD),e.blendFuncSeparate(e.ONE,e.ONE,e.ONE,e.ONE),oe.renderer.render(Z.mapSize,Z.mapSize),e.disable(e.BLEND),oe.fb.swap(),oe.blurTexture=te(e,0,oe.fb.read.targetTexture,oe.blur,Z.mapSize,Z.mapSize)};const se={},ce=n=>{const r=Z.particles;se.renderer=s(n,{vert:e("#version 300 es\n#define GLSLIFY 1\n\nlayout(location=0) in vec2 a_position;\nlayout(location=1) in vec2 a_velocity;\n\nuniform vec2 u_screensize;\n\nout vec2 state;\nout vec2 statedv;\n\nvoid main() {\n  gl_PointSize = 1.;\n  state = a_position / u_screensize; // Have to include this here for android for some reason\n  statedv = a_velocity;\n  gl_Position = vec4( a_position / u_screensize * 2. - 1., 0., 1. );\n}"),frag:e("#version 300 es\nprecision highp float;\n#define GLSLIFY 1\n\nin vec2 state; // Have to include this here for android for some reason\nin vec2 statedv;\n\nuniform float u_scale;\n\nout vec4 color;\n\nvoid main() {\n  color = vec4(1, 0, clamp(statedv, -1e6, 1e6 )) * u_scale;\n}"),count:r,mode:n.POINTS}),se.fb=c(n,{width:Z.mapSize,height:Z.mapSize,tiling:n.REPEAT,type:Z.fbType,internalFormat:Z.fbFormat,singular:!0}),se.blur=ne(n)};const le=(e,n)=>{se.renderer.bindVAOs(),e.bindVertexArray(Ue.renderer.readTFB),e.bindFramebuffer(e.FRAMEBUFFER,se.fb.read.fb),se.renderer.bindUniform("u_screensize",[Z.height,Z.height]),se.renderer.bindUniform("u_scale",Z.mapScale),e.clear(e.DEPTH_BUFFER_BIT),e.clear(e.DEPTH_BUFFER_BIT|e.COLOR_BUFFER_BIT),e.enable(e.BLEND),e.blendEquationSeparate(e.FUNC_ADD,e.FUNC_ADD),e.blendFuncSeparate(e.ONE,e.ONE,e.ONE,e.ONE),se.renderer.render(Z.mapSize,Z.mapSize),e.disable(e.BLEND),se.fb.swap(),se.blurTexture=te(e,0,se.fb.read.targetTexture,se.blur,Z.mapSize,Z.mapSize)};const fe=new Float32Array([-1,-1,3,-1,-1,3]),de=new Float32Array([0,0,2,0,0,2]),ue={},ve=e=>{ue.renderer.bindVAOs(),e.bindFramebuffer(e.FRAMEBUFFER,ue.fb.write.fb),e.activeTexture(e.TEXTURE0),e.bindTexture(e.TEXTURE_2D,ue.fb.read.targetTexture),ue.renderer.bindUniform("u_offset",[300*fxrand(),300*fxrand()]),ue.renderer.bindUniform("u_resolution",[ue.fb.width,ue.fb.height]),ue.renderer.render(ue.fb.width,ue.fb.height),ue.fb.swap()};const pe=new Float32Array(4*(Z.obstacleSize*Z.obstacleSize)).fill(0);let me=Z.height,xe=Z.height,_e=Z.obstacleSize,ye=Z.obstacleType,be=0;if(0==ye){let e=0;fxrand()<.35&&(e=parseInt(5*fxrand()));let n=.4*fxrand()+.35,r=.5*(1-n),t=n,o=.5*(1-t);for(let i=0;i<_e;i++){let a=((parseFloat(i)+.5)/parseFloat(_e)*n+r)*me*.8+.1*me;for(let n=0;n<_e;n++){let r=((parseFloat(n)+.5)/parseFloat(_e)*t+o)*xe*.8+.1*xe,s=4*be;pe[s]=a,pe[s+1]=r,pe[s+2]=550/parseFloat(_e),pe[s+3]=e,3==_e&&1==i&&1==n&&fxrand()<.5&&(pe[s+3]=parseInt(4*fxrand()+1),pe[s+2]=1200/parseFloat(_e)),_e>1&&0==Z.obstacleRands[be]&&(pe[s]=1e4),be++}}}else if(1==ye){let e=0;fxrand()<.6&&(e=parseInt(4*fxrand()));let n=300*fxrand()+250;for(let r=0;r<_e;r++)for(let r=0;r<_e;r++){let r=4*be;pe[r]=me/2,pe[r+1]=0==be?.3*xe:1==be?.7*xe:1e4,pe[r+2]=n,pe[r+3]=e,be++}}else if(2==ye){let e=0;fxrand()<.25&&(e=parseInt(4*fxrand()));let n=0;fxrand()<.5&&(n=1);let r=me/2;for(let t=0;t<_e;t++)for(let t=0;t<_e;t++){let t=4*be;pe[t]=(.95*fxrand()+.025)*me;let o=(.4*fxrand()+.3)*xe;1==n&&(pe[t]=(.4*fxrand()+.3)*me,o=(.95*fxrand()+.25)*xe),pe[t+1]=o,1==_e&&(pe[t]=r,o>.3*xe&&o<.7*xe&&(pe[t+1]=xe/2)),pe[t+2]=300*(.9*fxrand()+.1),1==_e&&(pe[t+2]=750*fxrand()+200),pe[t+3]=e,be++}}else if(3==ye){let e,n,r,t=0;fxrand()<.25&&(t=parseInt(4*fxrand()));let o=0;fxrand()<.5&&(o=1);let i=me/2;for(let a=0;a<_e;a++)for(let a=0;a<_e;a++){let a=4*be;0==be?(pe[a]=(.95*fxrand()+.025)*me,pe[a+1]=(.4*fxrand()+.3)*xe,1==o&&(pe[a]=(.4*fxrand()+.3)*me,pe[a+1]=(.95*fxrand()+.025)*xe),1==_e&&(pe[a]=i),pe[a+2]=750*(.8*fxrand()+.2),pe[a+3]=t,e=pe[a],n=pe[a+1],r=pe[a+2]):(pe[a]=(.95*fxrand()+.025)*me,pe[a+1]=(.5*fxrand()+.25)*xe,1==o&&(pe[a]=(.5*fxrand()+.25)*me,pe[a+1]=(.95*fxrand()+.025)*xe),pe[a+2]=200*(.9*fxrand()+.1),pe[a+3]=t),be++}}else for(let e=0;e<_e;e++)for(let e=0;e<_e;e++){let e=4*be;pe[e]=(.9*fxrand()+.05)*me,pe[e+1]=(.9*fxrand()+.05)*xe,pe[e+2]=475*(.5*fxrand()+.5)/parseFloat(_e),pe[e+3]=0,0==be&&(pe[e]=me/2,pe[e+1]=150*fxrand()+250,fxrand()<.5&&(pe[e+1]=xe-pe[e+1]),pe[e+2]=200*fxrand()+100),be++}const ge={obstacleData:pe},he=new Float32Array([-1,-1,3,-1,-1,3]),Te=new Float32Array([0,0,2,0,0,2]),De={},Ee=e=>{De.renderer.bindVAOs(),e.bindFramebuffer(e.FRAMEBUFFER,De.fb.write.fb),e.activeTexture(e.TEXTURE0),e.bindTexture(e.TEXTURE_2D,De.fb.read.targetTexture),e.activeTexture(e.TEXTURE1),e.bindTexture(e.TEXTURE_2D,De.obstacleTexture),De.renderer.bindUniform("u_framebuffer",0),De.renderer.bindUniform("u_obstaclesTex",1),De.renderer.bindUniform("u_obsRes",Z.obstacleSize),De.renderer.bindUniform("u_obsInvRes",1/parseFloat(Z.obstacleSize)),De.renderer.bindUniform("u_resolution",[De.fb.width,De.fb.height]),De.renderer.render(De.fb.width,De.fb.height),De.fb.swap()};const ze=new Float32Array([-1,-1,3,-1,-1,3]),we=new Float32Array([0,0,2,0,0,2]),Fe={},Re=e=>{Fe.renderer.bindVAOs(),e.bindFramebuffer(e.FRAMEBUFFER,Fe.fb.write.fb),e.activeTexture(e.TEXTURE0),e.bindTexture(e.TEXTURE_2D,Fe.fb.read.targetTexture),Fe.renderer.bindUniform("u_framebuffer",0),Fe.renderer.bindUniform("u_numForces",Z.numForces),Fe.renderer.bindUniform("u_canvas_res",[Fe.fb.width,Fe.fb.height]),Fe.renderer.bindUniform("u_rf0",Z.rf0),Fe.renderer.bindUniform("u_rf1",Z.rf1),Fe.renderer.render(Fe.fb.width,Fe.fb.height),Fe.fb.swap()},Ue={},Pe=1/Z.mapSize,Ae=n=>{const r=Z.particles,t=new Float32Array(2*r),o=new Float32Array(2*r),i=new Float32Array(4*r),a=new Float32Array(4*r);for(let e=0;e<2*r;e+=2)t[e]=fxrand()*Z.height,t[e+1]=fxrand()*Z.height,o[e]=2*fxrand()-1+.001,o[e+1]=2*fxrand()-1+.001,i[2*e]=1,i[2*e+1]=1,i[2*e+2]=1,i[2*e+3]=1,a[2*e]=-1,a[2*e+1]=15*fxrand()+5,a[2*e+2]=fxrand(),a[2*e+3]=fxrand()+1;Ue.renderer=s(n,{vert:e("#version 300 es\n#define GLSLIFY 1\n\nlayout(location=0) in vec2 a_position;\nlayout(location=1) in vec2 a_velocity;\nlayout(location=2) in vec4 a_forces;\nlayout(location=3) in vec4 a_extras;\n\nout vec2 v_position;\nout vec2 v_velocity;\nout vec4 v_forces;\nout vec4 v_extras;\nout vec3 v_color;\n\nuniform sampler2D u_particlemap;\nuniform sampler2D u_dvmap;\nuniform sampler2D u_sdfTex;\nuniform sampler2D u_forceTex;\nuniform sampler2D u_noiseTex;\n\nuniform float u_renderPer;\nuniform vec2 u_resolution;\nuniform float u_dpr;\nuniform float u_aspect;\nuniform float u_aspectOffset;\nuniform float u_time;\nuniform float u_frame;\nuniform float u_rf0;\nuniform float u_rf1;\n\nuniform int u_exteriorType;\nuniform int u_interiorType;\n\nuniform float u_rand;\nuniform vec2 u_invRes;\nconst float dt = 0.0002;\nconst float u_leading = 0.1;\nconst float u_speedMax = 5.0;\nconst float u_speedMin = 0.01;\n\nuniform vec3 u_c0;\nuniform vec3 u_c1;\nuniform vec3 u_c2;\nuniform vec3 u_c3;\n\nuniform int u_shellType;\n\n#define PI 3.14159265359\n#define TAU 6.28318530718\n\nvec3 getNormal( vec4 sdf ){\n    float x     = dot( vec2( 1.0, 0.0 ), sdf.yz );\n    float y     = dot( vec2( 0.0, 1.0 ), sdf.yz );\n    float z     = 1.0 - ( sdf.w - abs( sdf.x ) )/sdf.w;\n    vec3 n      = normalize( vec3( sqrt( abs( x ) ) * sign( x ) * ( 1.0 - z ), sqrt( abs( y ) ) * sign( y ) * ( 1.0 - z ), z ) );\n    return n;\n}\nvec3 quadColorLoop( vec3 c0, vec3 c1, vec3 c2, vec3 c3, float per ){\n    vec3 c = c0;\n    if( per < 0.25 ){\n        c = mix( c0, c1, per * 4.0 );\n    } else if( per < 0.5 ){\n        c = mix( c1, c2, ( per - 0.25 ) * 4.0 );\n    } else if( per < 0.75 ){\n        c = mix( c2, c3, ( per - 0.5 ) * 4.0 );\n    } else {\n        c = mix( c3, c0, ( per - 0.75 ) * 4.0 );\n    }\n    return c;\n}\n\n// Grab from https://www.shadertoy.com/view/4djSRW\n#define MOD3 vec3(.1031,.11369,.13787)\nfloat hash12(vec2 p) {\n    vec3 p3  = fract(vec3(p.xyx) * .1031);\n    p3 += dot(p3, p3.yzx + 33.33);\n    return fract((p3.x + p3.y) * p3.z);\n}\nvec3 hash33(vec3 p3) {\n\tp3 = fract(p3 * MOD3);\n    p3 += dot(p3, p3.yxz+19.19);\n    return -1.0 + 2.0 * fract(vec3((p3.x + p3.y)*p3.z, (p3.x+p3.z)*p3.y, (p3.y+p3.z)*p3.x));\n}\nvec2 hash22(vec2 p) {\n\tvec3 p3 = fract(vec3(p.xyx) * vec3(.1031, .1030, .0973));\n    p3 += dot(p3, p3.yzx+33.33);\n    return fract((p3.xx+p3.yz)*p3.zy);\n}\n\nvec4 derivative( vec4 state, inout float density, inout float densityMult, inout vec4 noiseData ){\n    vec2 uv             = state.xy;\n\n    // sample density and velocity\n    vec4 dv             = texture( u_dvmap, uv );\n    density             = dv.x;\n    vec2 avgVelocity    = dv.zw / max( density, 1.0 );\n\n    // RH sample the sdf\n    vec4 sdfData        = texture( u_sdfTex, uv );\n    float sdfDist       = sdfData.x;\n    vec2 sdfGrad        = sdfData.yz;\n    vec2 sdfPerp        = vec2( sdfGrad.y, -sdfGrad.x );\n\n    // RH sample the forces\n    vec4 forceData \t    = texture( u_forceTex, uv );\n    vec2 forceGrad      = forceData.xy;\n    vec2 forcePerp\t    = vec2( forceGrad.y, -forceGrad.x );\n    if( u_rf1 > 0.5 ){\n        forcePerp\t    = vec2( -forceGrad.y, forceGrad.x );\n    }\n    float forceDist\t    = forceData.z;\n\n    // get noises\n\tfloat n0\t\t    = noiseData.x;\n\tfloat n1\t\t    = noiseData.y;\n\tfloat n2\t\t    = noiseData.z;\n    float n3            = noiseData.w;\n    float noiseAngle    = ( n1 ) * TAU * 55.0;\n    vec2 noiseVel       = vec2( cos( noiseAngle ), sin( noiseAngle ) );\n    vec2 noisePerp      = vec2( noiseVel.y, -noiseVel.x );\n\n    vec3 normal         = getNormal( sdfData );\n\n    // set forces\n    vec4 forces         = a_forces;\n    float repel         = forces.x;\n    float attract       = forces.y;\n    float align         = forces.z;\n    float velEnforce    = forces.w;\n    float leading       = u_leading;\n\n    // set age\n    vec4 extras         = a_extras;\n    float age           = extras.x;\n    float life          = extras.y;\n    float rf0           = extras.z;\n    float pointSize     = extras.w;\n\n    vec2 customVel      = vec2( 0.0, 0.0 );\n\n    // BEGIN CUSTOM MODE DEFINITIONS\n    if( sdfDist > 0.0 ){    // exterior modes\n        if( u_exteriorType == 0 ){\n            leading         = sin( sdfDist * ( u_rf1 * 0.02 + 0.01 ) ) * 0.2 + 0.25;\n            attract         += sdfDist * 0.001;\n            align           *= 0.5;\n            repel           *= 0.1;\n            if( leading < 0.15 ){\n                leading = 0.005;\n            }\n\n            customVel       += forceGrad * ( sin( sdfDist * 0.05 ) * 0.5 + 0.5 ) * 5000.0;\n            customVel       -= sdfPerp * 100.0 * sin( sdfDist * 0.05 );\n\n        } else if( u_exteriorType == 1 ){\n            leading         = ( sin( forceGrad.x * 4.0 * sdfGrad.x * 2.0 + n1 * 2.0 ) ) + 0.5;\n            attract         += sin( density * sdfDist * 0.01 ) * 0.1 + 0.11;\n            repel           = 0.3;\n            velEnforce      = 0.2;\n\n            customVel       += forceGrad * ( sin( forceDist * 0.05 ) * 0.5 + 0.5 ) * 2000.0;\n            customVel       -= sdfPerp * 2200.0 * sin( sdfDist * 0.05 );\n\n        } else if( u_exteriorType == 2 ){\n            //align           = n2 + 1.5;//sin( forceDist * 0.02 ) * 0.3 + 0.31;\n\t\t\tleading \t    += sin( sdfPerp.x * 35.0 ) * sin( sdfPerp.y * 8.0 ) * 0.295 - 0.02;\n\n            if( leading < 0.0 ){\n                repel  \t\t    = 550.51;// n0 - 0.001;\n\t\t\t    attract  \t    = n0 * forceDist * 10.01;\n                densityMult     = 0.025;\n            } else {\n                repel  \t\t    = 0.3;// n0 - 0.001;\n                attract  \t    = n0 * forceDist * 0.002 + 0.25;\n                customVel       += noiseVel * 5350.0;\n                //customVel       -= sdfGrad * 5000.0;\n                align           = 0.1;\n                densityMult     = 0.04;\n                leading         = 5.0;\n            }\n\n            \n\n        } else if( u_exteriorType == 3 ){\n            repel           = 0.05 * ( pow( u_renderPer, 2.0 ) );//density * 0.0001 + pow( u_renderPer, 1.0 ) * 0.1;\n            attract         = sin( forceDist * 0.02 * n1 ) * 0.1 + 0.05;\n            align           = 0.1;\n            leading         = 0.1;//pow( u_renderPer, 2.0 ) * 0.005;//( sin( sdfDist * n1 * 0.4 + sin( forceGrad.y * 15.0 ) * 2.0 ) ) * 0.2 + 0.1;\n\n            customVel       += noiseVel * 5550.0;\n            customVel       += sdfPerp * pow( smoothstep( 2000.0, 0.0, sdfDist ), 11.0 ) * 155000.0 * ( 1.0 - u_renderPer );\n            if( rf0 < 0.5 ){\n                customVel       += vec2( 1.0, 0.0 ) * 3000.0;\n            } else {\n                customVel       -= vec2( 1.0, 0.0 ) * 3000.0;\n            }\n            \n            \n            \n        } else if( u_exteriorType == 4 ){\n            if( rf0 < 0.2 ){\n                attract         = 0.0;//u_renderPer * 0.05;//pow( u_renderPer, 2.0 ) * 0.4;\n                repel           = 0.25;//pow( 1.0 - u_renderPer, 2.0 ) * 0.2;\n                align           = sin( sdfDist * 0.05 ) * 0.2 + 1.3;\n                leading         = 0.01;//sin( sdfPerp.x * 35.0 * n1 ) * 0.5 + n0 * 0.1;\n\n                densityMult     = smoothstep( 0.0, 100.0, sdfDist ) * 0.02 * ( n1 + n0 );\n            } else if( rf0 < 0.3 ){\n                repel           = 0.0;// u_renderPer * 4.0;\n                attract         = 0.3;\n                leading         = 0.02;\n                densityMult     = 0.02;\n            } else {\n                customVel       += forceGrad * 15000.0;\n                densityMult     = 0.01;\n                /*\n                repel           = 0.2;\n                attract         = 0.3;\n                align           = 0.25;\n                leading         = 0.51;\n                densityMult     = 0.05;*/\n            }\n        } else if( u_exteriorType == 5 ){\n            attract         = sdfDist * 0.005;//pow( u_renderPer, 2.0 ) * 0.4;\n            align           = 0.5;\n            repel           = 0.025 + u_renderPer * 0.025;//pow( 1.0 - u_renderPer, 2.0 ) * 0.2;\n            leading         = sin( sdfPerp.x * 750.0 * u_dpr * n0 ) * 0.5 + 0.5 + n2 * 2.3;\n            densityMult     = ( sin( sdfDist * 0.1 + forceDist * 0.1 ) * 0.5 + 0.5 ) * 0.05 * ( 1.0 - u_renderPer * 0.1 );\n\n            customVel       += sdfPerp * pow( smoothstep( 500.0 * u_dpr, 0.0, sdfDist ), 25.0 ) * 25000.0;\n        } else if( u_exteriorType == 6 ){\n            attract         = 0.5 * n1 * pow( sin( u_renderPer * PI ), 2.0 );\n            align           = 0.1;\n            repel           = pow( u_renderPer, 2.0 ) * 0.1;\n            leading         = sin( forcePerp.y * 15.0 * n0 ) * 0.1 + n2 * 0.3;\n\n            customVel       += normal.xy * 125500.0 * u_renderPer * n2;\n        } else if( u_exteriorType == 7 ){\n            //attract         = 0.1 * n1 * pow( sin( u_renderPer * PI ), 2.0 );\n            align           = sin( density * n2 ) * 0.1 + 0.15;\n            repel           = u_renderPer * 0.5;\n            attract         = 1.5 * ( 1.0 - u_renderPer ) * sin( forceDist * 0.04 );\n            customVel       = ( sdfPerp + dv.zw ) * u_renderPer * 5.0;\n            //densityMult     = sin( forceDist * 0.01 ) * 0.202 + 0.202;\n        } else if( u_exteriorType == 8 ){\n            repel  \t\t    = n2 - n1 + u_renderPer * n0 + 0.5;\n\t\t\tattract  \t    = n1 * sdfDist * 0.05;\n            align           = n2 + 0.5;//sin( forceDist * 0.02 ) * 0.3 + 0.31;\n\t\t\tleading \t    += sin( sdfDist * 0.035 ) * 0.1 + 0.3;\n\n            customVel       += forceGrad * 100.0;\n            densityMult     = 0.005;\n        } else if( u_exteriorType == 9 ){\n            repel           = 0.01 + forceDist * 0.0001;\n            attract         = sin( forceGrad.y * sdfDist * 0.01 ) * 5.0 + 1.0;\n            align           = 1.0 * ( 1.0 - u_renderPer );\n            leading         = sin( sdfPerp.y * 2.0 * sdfDist * 0.01 ) * 0.01 - 0.005;\n        }\n\n    } else {                // interior modes\n        if( u_interiorType == 0 ){\n            leading         = sin( sdfDist * ( u_rf1 * 0.02 + 0.01 ) ) * 0.2 + 0.25;\n            attract         += sdfDist * 0.001;\n            align           *= 0.5;\n            repel           *= 0.1;\n            if( leading < 0.15 ){\n                leading = 0.005;\n            }\n\n            customVel       += forceGrad * ( sin( sdfDist * 0.05 ) * 0.5 + 0.5 ) * 5000.0;\n            customVel       -= sdfPerp * 100.0 * sin( sdfDist * 0.05 );\n\n        } else if( u_interiorType == 1 ){\n            leading         = ( sin( forceGrad.x * 4.0 * sdfGrad.x * 2.0 + n1 * 2.0 ) ) + 0.5;\n            attract         += sin( density * sdfDist * 0.01 ) * 0.1 + 0.11;\n            repel           = 0.3;\n            velEnforce      = 0.2;\n\n            customVel       += forceGrad * ( sin( forceDist * 0.05 ) * 0.5 + 0.5 ) * 2000.0;\n            //customVel       -= sdfPerp * 2200.0 * sin( sdfDist * 0.055 );\n\n        } else if( u_interiorType == 2 ){\n\t\t\trepel  \t\t    = n0;\n\t\t\tattract  \t    = n1 * forceDist * 0.025;\n            //align           = n2 + 0.5;//sin( forceDist * 0.02 ) * 0.3 + 0.31;\n\t\t\tleading \t    += sin( sdfPerp.x * 50.0 ) * 0.5;\n\n            //customVel       += forceGrad * 1000.0;\n            //densityMult     = 0.02;\n\n        } else if( u_interiorType == 3 ){\n            repel           = 0.0;//density * 0.0001 + pow( u_renderPer, 1.0 ) * 0.1;\n            attract         = sin( forceDist * 0.02 * n1 ) * 0.1;\n            align           = 0.1;\n            leading         = 0.1;//pow( u_renderPer, 2.0 ) * 0.005;//( sin( sdfDist * n1 * 0.4 + sin( forceGrad.y * 15.0 ) * 2.0 ) ) * 0.2 + 0.1;\n\n            customVel       += noiseVel * 50.0;\n            customVel       += sdfPerp * pow( smoothstep( 300.0, 0.0, sdfDist ), 5.0 ) * 15000.0;\n        } else if( u_interiorType == 4 ){\n            repel           = u_renderPer * 4.0;\n            attract         = ( 1.0 - u_renderPer ) * 0.3;\n            leading         = 0.02;\n            densityMult     = 0.05;\n\n        } else if( u_interiorType == 5 ){\n            attract         = sdfDist * 0.01;//pow( u_renderPer, 2.0 ) * 0.4;\n            align           = 0.21;\n            repel           = 0.02 + u_renderPer * 0.001;//pow( 1.0 - u_renderPer, 2.0 ) * 0.2;\n            leading         = sin( sdfPerp.x * 1555.0 * n0 ) * 0.5 + 0.5 + n2 * 2.3;\n            densityMult     = ( sin( sdfDist * 0.1 + forceDist * 0.1 ) * 0.5 + 0.5 ) * 0.05 * ( 1.0 - u_renderPer * 0.1 );\n\n            customVel       -= sdfPerp * pow( smoothstep( 1800.0, 0.0, sdfDist ), 25.0 ) * 25000.0;\n        } else if( u_interiorType == 6 ){\n            attract         = 0.5 * n1 * pow( sin( u_renderPer * PI ), 2.0 );\n            align           = 1.0;\n            repel           = sin( pow( u_renderPer, 1.0 ) * PI ) * 0.5 + 0.25;\n            leading         = sin( forcePerp.y * 1555.0 * n0 ) * 0.1 + n2 * 0.3;\n\n            //customVel       += normal.xy * 255500.0 * u_renderPer * n2;\n\n            float borderForce = smoothstep( 170.0, 0.0, abs( sdfDist ) );\n            customVel       -= sdfGrad * borderForce * 40550.0;\n            densityMult     = 0.1;\n        } else if( u_interiorType == 7 ){\n            //attract         = 0.1 * n1 * pow( sin( u_renderPer * PI ), 2.0 );\n            align           = sin( density * n2 ) * 0.1 + 0.15;\n            repel           = u_renderPer * 1.0;\n            attract         = 25.5 * ( 1.0 - u_renderPer );\n            customVel       = ( sdfPerp + dv.zw ) * u_renderPer * 25.0 * sin( sdfDist * 0.01 );\n        } else if( u_interiorType == 8 ){\n            repel  \t\t    = n0 * ( u_renderPer );\n\t\t\tattract  \t    = n1 * forceDist * 0.01;\n            align           = n2 + 0.5;//sin( forceDist * 0.02 ) * 0.3 + 0.31;\n\t\t\tleading \t    += sin( sdfDist * 0.035 ) * 0.1 + 0.3;\n\n            customVel       += forceGrad * 1000.0;\n            //densityMult     = 0.02;\n        } else if( u_interiorType == 9 ){\n            repel           = 0.01 + forceDist * 0.0001;\n            attract         = sin( forceGrad.y * sdfDist * 0.01 ) * 2.0 + 1.0;\n            align           = 2.0;\n            leading         = sin( sdfPerp.y * 2.0 * sdfDist * 0.01 ) * 0.01 - 0.005;\n        }\n        \n    }\n    // END CUSTOM MODE DEFINITIONS\n\n    //customVel += vec2( 0.0, n2 * 155.0 );\n    vec2 xy             = state.xy + leading * dt * state.zw;\n\n    // Sample the blurred density and velocity\n    vec3 dvm            = texture( u_dvmap, xy ).xzw;\n    xy                  = state.xy + dt * 2.0 * leading * dvm.yz / dvm.x;\n\n    // Density gradient calculation\n    float densityE      = texture( u_dvmap, state.xy + vec2( u_invRes.x, 0 ) ).x;\n    float densityW      = texture( u_dvmap, state.xy + vec2( -u_invRes.x, 0 ) ).x;\n    float densityN      = texture( u_dvmap, state.xy + vec2( 0, u_invRes.y ) ).x;\n    float densityS      = texture( u_dvmap, state.xy + vec2( 0, -u_invRes.y ) ).x;\n    vec2 densityGrad    = vec2( densityE - densityW, densityN - densityS ) / u_invRes;\n\n    vec4 avgPositionValues  = texture( u_particlemap, xy );\n    float density1          = texture( u_dvmap, xy ).x;\n\n    // Divide the sum of velocities by the sum of masses to compute a mass-weighted\n    // local average position\n    vec2 avgPosition = vec2(\n        state.x < 0.5 ? avgPositionValues.x : avgPositionValues.z,\n        state.y < 0.5 ? avgPositionValues.y : avgPositionValues.w\n    ) / max( density1, 1e-8 );\n\n    // The magic numbers!\n    float repulsion     = 1.4 * repel;\n    float follow        = 5000.0 * align;\n    float cohesion      = 1700000.0 * attract;\n    float velocityEnforcement = 380.0 * velEnforce;\n\n    float vmag          = length( state.zw );\n\n    vec2 v              = -densityGrad * repulsion\n                        + customVel\n                        + ( avgVelocity - state.zw ) * follow\n                        + ( avgPosition - state.xy ) * cohesion / max( 0.01, leading )\n                        + state.zw * ( u_speedMax - vmag ) * velocityEnforcement\n                        - 100.0 * length( state.xy - 0.5 ) * ( state.xy - 0.5 );\n\n    return vec4( state.zw, v );\n}\n\nvoid main() {\n    float densityMult   = 0.01;\n    float density       = 0.0;\n\n    vec4 state          = vec4( a_position / u_resolution, a_velocity );\n    \n    vec4 noiseData\t    = texture( u_noiseTex, state.xy );\n    state               += dt * derivative( state, density, densityMult, noiseData );\n    vec2 uv             = state.xy;\n    vec2 pos            = state.xy * u_resolution;\n    vec2 vel            = state.zw;\n\n    // get noises\n    float n0\t\t= noiseData.x;\n\tfloat n1\t\t= noiseData.y;\n\tfloat n2\t\t= noiseData.z;\n    float n3        = noiseData.w;\n\n    // get extras\n    vec4 extras     = a_extras;\n    float age       = extras.x;\n    float life      = extras.y;\n    float rf0       = extras.z;\n    float pointSize = extras.w;\n\n    // limit speed\n    float speed     = length( vel );\n    float maxSpeed  = u_speedMax + density * densityMult;\n    if( speed > maxSpeed ){\n        vel         = normalize( vel ) * maxSpeed;\n    } else if( speed < u_speedMin ){\n        vel         = normalize( vel ) * u_speedMin;\n    }\n    \n    // CHECK FOR POINTS BEING OUT OF BOUNDS (OOB)\n    int oob = 0;\n\n    // First check, the boundaries of the window\n    float b = 50.0 * ( n2 * 0.5 + 0.5 );\n    float bwo = ( u_resolution.y * ( 1.0 - u_aspect ) ) * 0.5;\n    if( pos.y < b || pos.y > u_resolution.y - b ) oob = 1;\n    if( pos.x < bwo + b || pos.x > u_resolution.x - bwo - b ) oob = 1;\n\n    // Second check, the particle age\n    if( age < 0.0 || age > life ) oob = 1;\n\n    // Third check, interior sdf border\n    vec4 sdfData    = texture( u_sdfTex, uv );\n    float sdfDist   = sdfData.x;\n    float minBorder = 65.0 * ( rf0 * 0.25 + 0.75 );\n    float maxBorder = 150.0 * ( rf0 * 0.25 + 0.75 );\n    float thresh    = sin( smoothstep( -minBorder * rf0, maxBorder * rf0, sdfDist ) * PI );\n    if( thresh > n3 ) oob = 1;\n\n    // if oob, respawn\n    if( oob == 1 ){\n        pos         = hash22( pos ) * u_resolution;\n\n        if( u_renderPer > 0.2 ){\n            pos.x += 5000.0;\n        }\n    }\n\n    // set position and velocity\n    v_position      = pos;\n    v_velocity      = vel * ( pow( 1.0 - u_renderPer, 2.0 ) * 0.9 + 0.1 ); // adjust velocity\n\n    if( u_frame < 5.0 ) {\n        v_velocity  = a_velocity;\n    }\n    \n    vec2 position   = a_position / u_resolution * 2.0 - 1.0;\n    v_forces        = a_forces;\n    v_extras        = a_extras;\n    if( oob == 1 ){\n        v_extras.x  = 0.0; // reset age\n    }\n    v_extras.x      += 0.0333;\n\n    // set velocity color\n    vec2 velDir     = normalize( v_velocity.xy );\n    float angle     = atan( velDir.y, velDir.x );\n    v_color         = quadColorLoop( u_c0, u_c1, u_c2, u_c3, fract( angle / TAU ) );\n    \n    // set pointsize\n    float fadeIn    = min( age * 3.0, 1.0 );\n    float fadeOut   = pow( 1.0 - u_renderPer, 0.5 );\n    float ps        = max( fadeIn * fadeOut * pointSize, 0.2 );\n\n    gl_PointSize    = ps;\n    gl_Position     = vec4( position, 0.0, 1.0 );\n}"),frag:e("#version 300 es\nprecision highp float;\n#define GLSLIFY 1\n\nin vec3 v_color;\n\nout vec4 color;\n\nuniform vec2 u_resolution;\nuniform float u_time;\nuniform float u_renderPer;\n\nvoid main() {\n    vec2 uv = gl_PointCoord.xy - .5;\n\n    //float opacity = clamp(smoothstep(.5, 0., length(uv)), 0., 1.);\n    float opacity   = clamp( smoothstep( 0.5, 0.1, length(uv) ), 0.0, 1.0 ) * pow( 1.0 - u_renderPer, 0.5 );\n    color = vec4( v_color, 1.0 ) * opacity;\n}"),attributes:{a_forces:{data:i,size:4},a_extras:{data:a,size:4}},transformFeedbacks:{a_position:{data:t,size:2,usage:n.STREAM_COPY,varying:"v_position"},a_velocity:{data:o,size:2,usage:n.STREAM_COPY,varying:"v_velocity"},a_forces:{data:i,size:4,usage:n.STREAM_COPY,varying:"v_forces"},a_extras:{data:a,size:4,usage:n.STREAM_COPY,varying:"v_extras"}},count:r,mode:n.POINTS}),Ue.fb=c(n,{width:Z.height,height:Z.height,type:Z.fbType,internalFormat:Z.fbFormat,singular:!0})};let Se=0,Ie=Z.interiorType,Me=Z.exteriorType;const Be=(e,n,r)=>{Ue.renderer.bindVAOs(),Ue.renderer.bindTransformFeedbacks(),e.bindFramebuffer(e.FRAMEBUFFER,Ue.fb.read.fb),e.activeTexture(e.TEXTURE0),e.bindTexture(e.TEXTURE_2D,oe.blurTexture),e.activeTexture(e.TEXTURE1),e.bindTexture(e.TEXTURE_2D,se.blurTexture),e.activeTexture(e.TEXTURE2),e.bindTexture(e.TEXTURE_2D,De.fb.read.targetTexture),e.activeTexture(e.TEXTURE3),e.bindTexture(e.TEXTURE_2D,Fe.fb.read.targetTexture),e.activeTexture(e.TEXTURE4),e.bindTexture(e.TEXTURE_2D,ue.fb.read.targetTexture),Ue.renderer.bindUniform("u_particlemap",0),Ue.renderer.bindUniform("u_dvmap",1),Ue.renderer.bindUniform("u_sdfTex",2),Ue.renderer.bindUniform("u_forceTex",3),Ue.renderer.bindUniform("u_noiseTex",4),Ue.renderer.bindUniform("u_time",n/1e3),Ue.renderer.bindUniform("u_frame",Se++),Ue.renderer.bindUniform("u_renderPer",r),Ue.renderer.bindUniform("u_rand",fxrand()),Ue.renderer.bindUniform("u_invRes",[Pe,Pe]),Ue.renderer.bindUniform("u_resolution",[Ue.fb.width,Ue.fb.height]),Ue.renderer.bindUniform("u_dpr",Z.dpr),Ue.renderer.bindUniform("u_aspect",Z.aspect),Ue.renderer.bindUniform("u_aspectOffset",(1-Z.aspect)/2),Ue.renderer.bindUniform("u_rf0",Z.rf0),Ue.renderer.bindUniform("u_rf1",Z.rf1),Ue.renderer.bindUniform("u_exteriorType",Me),Ue.renderer.bindUniform("u_interiorType",Ie),Ue.renderer.bindUniform("u_c0",Z.c0),Ue.renderer.bindUniform("u_c1",Z.c1),Ue.renderer.bindUniform("u_c2",Z.c2),Ue.renderer.bindUniform("u_c3",Z.c3),Ue.renderer.bindUniform("u_shellType",Z.shellType),e.clear(e.DEPTH_BUFFER_BIT),e.clear(e.DEPTH_BUFFER_BIT|e.COLOR_BUFFER_BIT),e.enable(e.BLEND),e.blendEquationSeparate(e.FUNC_ADD,e.MAX),e.blendFuncSeparate(e.ONE,e.ONE,e.ONE,e.ONE),Ue.renderer.render(Ue.fb.width,Ue.fb.height),e.disable(e.BLEND),Ue.fb.swap()};const Ce=new Float32Array([-1,-1,3,-1,-1,3]),Oe=new Float32Array([0,0,2,0,0,2]),Le={},Ge=n=>{Le.renderer=s(n,{vert:e($),frag:e("#version 300 es\nprecision highp float;\n#define GLSLIFY 1\nin vec2 v_uv;\nout vec4 color;\n\nuniform sampler2D u_framebuffer;\nuniform sampler2D u_points;\n\nuniform vec2 u_resolution;\nuniform vec2 u_invResolution;\nuniform int u_isRendering;\nuniform float u_renderPer;\n\nfloat off = 0.999;\nfloat off2 = 0.0005;\n\nvec4 blur( float off, float multi ) {\n    vec4 col = vec4( 0. );\n    for(float x = -off; x <= off; x++) {\n        for(float y = -off; y <= off; y++) {\n            col += texture( u_framebuffer, v_uv+vec2(x,y)*u_invResolution );\n        }\n    }\n    col /= multi;\n    return col;\n}\n\nvoid main() {\n\tvec2 uv\t\t\t= gl_FragCoord.xy/u_resolution;\n  \tvec4 frag \t\t= texture( u_points, v_uv );\n  \tvec4 prev \t\t= texture( u_framebuffer, v_uv );\n\tfloat decay \t= 0.1;\n \n    if( u_isRendering == 1 ){\n        if( u_renderPer > 0.0 ){\n            if( u_renderPer < 0.2 ) prev = blur( 3.0, 49.0 );\n            else if( u_renderPer < 0.4 ) prev = blur( 2.0, 25.0 );\n            else if( u_renderPer < 0.8 ) prev = blur( 1.0, 9.0 );\n\n            float multi = 0.25;\n            color = prev * ( 0.995 + u_renderPer * 0.005 ) + frag * multi;\n            color = clamp( color, vec4( 0.0 ), vec4( 1.0 ) );\n            if( u_renderPer > 0.999 ){\n                color = prev;\n            }\n        } else {\t\t// LIVE VIEW\n            color = prev * decay + frag;\n        }\n    } else {\n        color = prev * decay + frag;\n    }\n    color = clamp( color, vec4( 0.0 ), vec4( 1.0 ) );\n}"),attributes:{position:{data:Ce,size:2},uv:{data:Oe,size:2}},count:3}),Le.fb=c(n,{width:Z.height,height:Z.height,type:Z.fbType,internalFormat:Z.fbFormat})},Ve=(e,n,r)=>{Le.renderer.bindVAOs(),e.bindFramebuffer(e.FRAMEBUFFER,Le.fb.write.fb),e.activeTexture(e.TEXTURE0),e.bindTexture(e.TEXTURE_2D,Le.fb.read.targetTexture),e.activeTexture(e.TEXTURE1),e.bindTexture(e.TEXTURE_2D,Ue.fb.read.targetTexture),Le.renderer.bindUniform("u_framebuffer",0),Le.renderer.bindUniform("u_points",1),Le.renderer.bindUniform("u_renderPer",n),Le.renderer.bindUniform("u_isRendering",r),Le.renderer.bindUniform("u_resolution",[Z.height,Z.height]),Le.renderer.bindUniform("u_invResolution",[1/Z.height,1/Z.height]),Le.renderer.render(Z.height,Z.height),Le.fb.swap()};const Xe=new Float32Array([-1,-1,3,-1,-1,3]),Ne=new Float32Array([0,0,2,0,0,2]),qe={},ke=n=>{qe.renderer=s(n,{vert:e($),frag:e("#version 300 es\nprecision highp float;\n#define GLSLIFY 1\nin vec2 v_uv;\nout vec4 color;\n\nuniform sampler2D u_trailsTex;\nuniform sampler2D u_particlemap;\nuniform sampler2D u_dvmap;\nuniform sampler2D u_sdfTex;\nuniform sampler2D u_forceTex;\nuniform sampler2D u_noiseTex;\n\nuniform float u_rf0;\nuniform float u_rf1;\nuniform int u_colorMode;\nuniform int u_screenMode;\nuniform int u_shellType;\nuniform vec3 u_lightDir0;\nuniform vec3 u_lightDir1;\nuniform int u_mapId;\nuniform float u_renderPer;\nuniform float u_preloader;\nuniform float u_preloaderOut;\nuniform int u_paletteId;\nuniform vec3 u_cd0;\nuniform vec3 u_cd1;\nuniform vec3 u_cd2;\nuniform int u_colorId;\nuniform float u_saturation;\n\nuniform vec2 u_resolution;\nuniform int u_dpr;\n\n#define PI 3.14159265359\n#define TAU 6.28318530718\n\nfloat parabola( float x, float k ){\n    return clamp( pow( 4.0*x*(1.0-x), k ), 0.0, 1.0 );\n}\n\n// All components are in the range [0…1], including hue.\nvec3 rgb2hsv(vec3 c)\n{\n    vec4 K = vec4(0.0, -1.0 / 3.0, 2.0 / 3.0, -1.0);\n    vec4 p = mix(vec4(c.bg, K.wz), vec4(c.gb, K.xy), step(c.b, c.g));\n    vec4 q = mix(vec4(p.xyw, c.r), vec4(c.r, p.yzx), step(p.x, c.r));\n\n    float d = q.x - min(q.w, q.y);\n    float e = 1.0e-10;\n    return vec3(abs(q.z + (q.w - q.y) / (6.0 * d + e)), d / (q.x + e), q.x);\n}\n\n// All components are in the range [0…1], including hue.\nvec3 hsv2rgb(vec3 c)\n{\n    vec4 K = vec4(1.0, 2.0 / 3.0, 1.0 / 3.0, 3.0);\n    vec3 p = abs(fract(c.xxx + K.xyz) * 6.0 - K.www);\n    return c.z * mix(K.xxx, clamp(p - K.xxx, 0.0, 1.0), c.y);\n}\n\nvec3 triToneColor( vec3 c0, vec3 c1, vec3 c2, float per ){\n    vec3 c = c0;\n    if( per < 0.5 ){\n        c = mix( c0, c1, per * 2.0 );\n    } else {\n        c = mix( c1, c2, ( per - 0.5 ) * 2.0 );\n    }\n    return c;\n}\n\nfloat getGrayscale( vec3 c ){\n    return max( max( c.x, c.y ), c.z );\n}\n\nvec3 getNormal( vec4 sdf ){\n    float x     = dot( vec2( 1.0, 0.0 ), sdf.yz );\n    float y     = dot( vec2( 0.0, 1.0 ), sdf.yz );\n    float z     = 1.0 - ( sdf.w - abs( sdf.x ) )/sdf.w;\n    vec3 n      = normalize( vec3( x * ( 1.0 - z ), y * ( 1.0 - z ), z ) );\n    return n;\n}\n\nfloat hash11( float p ){\n    p = fract(p * .1031);\n    p *= p + 33.33;\n    p *= p + p;\n    return fract(p);\n}\n\nvoid main() {\n    vec2 uv             = gl_FragCoord.xy / u_resolution;\n    uv.x                = uv.x * 0.733 + 0.1335; // make variable\n \n    color               = texture( u_trailsTex, uv );\n    vec3 initColor      = color.xyz;\n    float bright        = getGrayscale( color.xyz );\n    vec3 hsv            = rgb2hsv( color.xyz );\n\n    // get sdf data\n    vec4 sdfData        = texture( u_sdfTex, uv );\n    float sdfDist       = sdfData.x;\n    float sdfDistSq     = sdfDist * sdfDist;\n    vec2 sdfGrad        = normalize( sdfData.yz );\n    vec2 sdfPerp        = vec2( sdfGrad.y, -sdfGrad.x );\n    float sdfRadius     = sdfData.w;\n\n    // get force data\n    vec4 forceData \t    = texture( u_forceTex, uv );\n    vec2 forceGrad      = normalize( forceData.xy );\n    vec2 forcePerp\t    = vec2( forceGrad.y, -forceGrad.x );\n    float forceDist\t    = forceData.z;\n\n    // get noise data\n    vec4 noiseData      = texture( u_noiseTex, uv );\n    float n0\t\t    = noiseData.x;\n    float n1\t\t    = noiseData.y;\n    float n2 \t\t    = noiseData.z;\n    float n3            = noiseData.w;\n\n    // get normal map\n    vec3 normal         = getNormal( sdfData );\n    //vec3 normalDetail   = texture( u_normalTex, uv ).xyz * vec3( 2.0 ) - vec3( 1.0 );\n\n    // lighting calculations\n    float center \t= parabola( -( sdfDist - 15.0 ) * 0.02, 1.0 );\t\n    float dp0       = clamp( dot( normalize( u_lightDir0 ), normal ), 0.0, 1.0 );\n    float dp1       = clamp( dot( normalize( u_lightDir1 ), normal ), 0.0, 1.0 );\n    float dp2       = clamp( dot( normalize( -u_lightDir0 ), normal ), 0.0, 1.0 );\n    float dpc\t\t= dp0 * center;\n\n    vec4 bg         = vec4( 0.02, 0.02, 0.02, 1.0 );\n\n    // COLORS\n    int cType = 0;\n    if( u_colorMode == 0 ){     // iridescence via chromatic abberation\n        vec2 uvr    = uv - normal.xy * max( 500.0 - forceDist, 0.0 ) * 0.00005 * sin( forceDist * 0.0002 );\n        vec2 uvg    = uv - normal.xy * max( 500.0 - forceDist, 0.0 ) * 0.00006 * sin( forceDist * 0.0003 );\n        vec2 uvb    = uv - normal.xy * max( 500.0 - forceDist, 0.0 ) * 0.00007 * sin( forceDist * 0.0004 );\n        color.x     = texture( u_trailsTex, uvr ).x;\n        color.y     = texture( u_trailsTex, uvg ).y;\n        color.z     = texture( u_trailsTex, uvb ).z;\n\n        color.xyz   = mix( vec3( bright ), color.xyz, u_saturation );\n\n    } else if( u_colorMode == 1 ){      // color three-tone (including b&w and w&b)\n        vec2 uvr    = uv - normal.xy * max( 100.0 - forceDist, 0.0 ) * 0.0001 * sin( forceDist * 0.0002 );\n        vec2 uvg    = uv - normal.xy * max( 100.0 - forceDist, 0.0 ) * 0.0002 * sin( forceDist * 0.0003 );\n        vec2 uvb    = uv - normal.xy * max( 100.0 - forceDist, 0.0 ) * 0.0003 * sin( forceDist * 0.0004 );\n        color.x     = texture( u_trailsTex, uvr ).x;\n        color.y     = texture( u_trailsTex, uvg ).y;\n        color.z     = texture( u_trailsTex, uvb ).z;\n\n        if( u_paletteId <= 5 ){\n            color.xyz   = triToneColor( u_cd0, u_cd1, u_cd2, pow( bright, 0.5 ) );\n        } else {\n            color.xyz   = triToneColor( u_cd0, u_cd1, u_cd2, pow( bright, 1.0 ) );\n        }\n        \n        bg.xyz      = u_cd0;\n\n    }\n\n    color.xyz = clamp( color.xyz, vec3( 0.0 ), vec3( 1.0 ) );\n\n    // SHELLS\n    if( u_shellType == 1 ){\t\t// glass shell\n        /*if( sdfDist < 0.0 ){ // inside\n            color.xyz += parabola( smoothstep( -sdfRadius*0.75, 0.0, sdfDist ), 3.0 ) * dp0 * ( n1 + n2 ) * 0.1;\n\n            color.xyz += pow( dp0, 255.0 ) * 1.0;\n            color.xyz += pow( dp0, 25.0 ) * 0.3 * ( n1 + n2 );\n\n            color.xyz += parabola( smoothstep( -50.0, -10.0, sdfDist ), 7.0 ) * pow( dp1, 255.0 ) * ( n0 + n1 );\n            color.xyz += parabola( smoothstep( -sdfRadius * 0.4, 0.0, sdfDist ), 4.0 ) * pow( dp1, 15.0 ) * ( n0 + n1 ) * 1.5;\n            color.xyz *= ( pow( dp0, 1.0 ) * 0.75 + 0.25 );\n\n            color.xyz += pow( color.xyz, vec3( 5.0 ) ) * 0.5;\n\n            vec3 reflection = texture( u_trailsTex, v_uv + normal.xy * ( -sdfDist * 0.001 ) ).xyz * pow( 1.0 - normal.z, 2.0 ) * 0.3;\n            if( u_paletteId == 5 ){ // if black...\n                color.xyz += getGrayscale( reflection );\n            } else if( u_paletteId < 5 ) {\n                color.xyz += reflection;\n            }\n\n        } else {                // outside\n            float sr = sin( smoothstep( -25.0 * n0 - 2.0, 25.0 * n0 + 2.0, sdfDist ) * PI ) ;\n            color.xyz -= sr * 0.1 * dp1;\n            color.xyz -= ( 1.0 - smoothstep( 0.0, 250.0, sdfDist ) ) * 0.2 * ( n2 + n1 ) * dp0;\n            color.xyz -= ( 1.0 - smoothstep( 0.0, 75.0 * n1, sdfDist ) ) * 0.5;\n\n            color.xyz *= mix( dp0 * 0.8 + 0.2, 1.0, ( smoothstep( 0.0, 400.0, sdfDist ) ) );\n\n            float m = ( 1.0 - smoothstep( 20.0, ( n1 + 0.3 ) * 160.0, sdfDist ) );\n            color.xyz -= m * 0.5 * n0;\n            color.xyz -= m * 0.5 * n1;\n        }*/\n\n        if( sdfDist < 0.0 ){ // inside\n            color.xyz += parabola( smoothstep( -sdfRadius*0.75, 0.0, sdfDist ), 3.0 ) * dp0 * ( n1 + n2 ) * 0.1;\n\n            color.xyz += pow( dp0, 255.0 );\n            color.xyz += pow( dp0, 25.0 ) * 0.3 * ( n1 + n2 );\n\n            color.xyz += parabola( smoothstep( -50.0, -10.0, sdfDist ), 7.0 ) * pow( dp1, 255.0 ) * ( n0 + n1 );\n            color.xyz += parabola( smoothstep( -sdfRadius * 0.4, 0.0, sdfDist ), 4.0 ) * pow( dp1, 15.0 ) * ( n0 + n1 ) * 1.5;\n            color.xyz *= ( pow( dp0, 1.0 ) * 0.75 + 0.25 );\n\n            color.xyz = pow( color.xyz, vec3( 3.0 ) ) * 0.7;\n\n            vec3 reflection = texture( u_trailsTex, v_uv + normal.xy * ( -sdfDist * 0.002 ) ).xyz * pow( 1.0 - normal.z, 15.0 ) * 0.3;\n            float grayReflect = getGrayscale( reflection );\n            if( u_colorId <= 5 ){\n                color.xyz   += triToneColor( u_cd0, u_cd1, u_cd2, pow( grayReflect, 0.5 ) );\n            } else if( u_colorId >= 7 ){\n                color.xyz    += texture( u_trailsTex, v_uv + normal.xy * ( -sdfDist * 0.002 ) ).xyz * pow( 1.0 - normal.z, 15.0 ) * 0.3;\n                //color.xyz   += triToneColor( u_cd0, u_cd1, u_cd2, pow( grayReflect, 0.5 ) );\n            }\n            \n            color.xyz   *= smoothstep( 0.0, -20.0, sdfDist );\n\n        } else {                // outside\n            float sr = sin( smoothstep( -25.0 * n0 - 2.0, 25.0 * n0 + 2.0, sdfDist ) * PI ) ;\n            color.xyz -= sr * 0.1 * dp0;\n            color.xyz -= ( 1.0 - smoothstep( 0.0, 150.0, sdfDist ) ) * 0.2 * ( n2 + n1 ) * dp0;\n            color.xyz -= ( 1.0 - smoothstep( 0.0, 75.0 * n1, sdfDist ) ) * 0.5;\n\n            color.xyz *= mix( dp1 * 0.8 + 0.2, 1.0, ( smoothstep( 0.0, 100.0, sdfDist ) ) );\n\n            float m = ( 1.0 - smoothstep( 20.0, ( n1 + 0.3 ) * 80.0, sdfDist ) );\n            color.xyz -= m * 0.025 * n0;\n            color.xyz -= m * 0.025 * n1;\n        }\n    }\n\n    if( u_colorMode == 0 ){\n        float brightness = 0.01;\n        float contrast = 1.005;\n        //color.rgb = pow( color.rgb, vec3( 2.0 ) );\n        color.rgb = ( ( color.rgb - 0.5 ) * max( contrast, 0.0 ) ) + 0.5; // contrast\n        color.rgb += brightness; // brightness\n\n        vec3 hsv = rgb2hsv( color.rgb );\n        hsv.y += 0.2;\n        color.rgb = hsv2rgb( hsv );\n    }\n\n        // SCREENS\n    if( sdfDist > 0.0 ){\n        if( u_screenMode == 0 ){\n            //color.xyz   *= ( n0 + n1 + n2 ) * vec3( sin( fract( gl_FragCoord.y / 3.0 ) * PI ) );\n\n        } else if( u_screenMode == 1 ){\n            color.xyz   *= vec3( sin( fract( gl_FragCoord.y / ( 4.0 ) ) * PI ) * 0.2 + 0.8 );\n\n        } else if( u_screenMode == 2 ){\n            color.xyz   *= vec3( sin( sdfDist * 1.1 ) * 0.05 + 1.0 );\n\n        } else if( u_screenMode == 3 ){\n            color.xyz   *= vec3( sin( forceDist * 1.1 ) * 0.05 + 1.0 );\n        }\n    }\n\n    if( u_paletteId != 6 ){\n        color += bg;\n    } else {\n        color += vec4( 0.0, 0.0, 0.0, 1.0 );\n    }\n\n    // show texture maps\n    if( u_mapId == 1 ){\n        color = vec4( sdfDist * 0.055, sdfGrad, 1.0 ) * 0.9;\n    } else if( u_mapId == 2 ){\n        color = vec4( noiseData );\n    } else if( u_mapId == 3 ){\n        color = vec4( sin( forceData.x * 3.0 ), sin( forceData.y * 3.0 ), sin( forceData.z * 0.35 ), forceData.w ) * 0.9;\n    } else if( u_mapId == 4 ){\n        color.xyz = normal;\n    }\n\n    if( u_preloaderOut < 0.999 ){\n        float dpr = float( u_dpr );\n        float mixPer = max( 0.0, ( u_preloaderOut - 0.5 ) ) * 2.0;\n        color = mix( bg, color, mixPer );\n\n        for( int i=0; i<10; i++ ){\n            float rnd   = hash11( float( i ) * 25.5 + u_rf0 * 25.0 );\n            float per1  = float( i ) / 10.0;\n            float th    = per1 * TAU + PI * 0.5;\n            float tha   = u_preloader * TAU + PI * 0.5;\n\n            float dotPer = fract( u_preloader * 10.0 );\n            int dotId    = int( floor( u_preloader * 10.0 ) );\n\n            vec2 c      = u_resolution/2.0;\n            float ro    = 35.0 * dpr;\n            float r     = ( 2.5 * dpr + rnd * 2.5 * dpr );\n            r           *= smoothstep( 0.5, 0.0, u_preloaderOut );\n\n            if( i == dotId ){\n                r *= dotPer;\n            }\n\n            vec2 p      = c + vec2( cos( th ), sin( th ) ) * ro;\n\n            float d     = distance( gl_FragCoord.xy, p );\n            if( d < r && tha > th ){\n                if( u_colorId != 6 ){ // if not white bg...\n                    color.xyz = vec3( smoothstep( r, r * 0.7, d ) );\n                } else {                // ... else invert colors\n                    color.xyz = vec3( 1.0 - smoothstep( r, r * 0.7, d ) );\n                }\n            } \n        }\n    }\n}"),attributes:{position:{data:Xe,size:2},uv:{data:Ne,size:2}},count:3})};let Ye=0;const He=(e,n,r,t,o)=>{qe.renderer.bindVAOs(),e.bindFramebuffer(e.FRAMEBUFFER,null),e.activeTexture(e.TEXTURE0),e.bindTexture(e.TEXTURE_2D,Le.fb.read.targetTexture),e.activeTexture(e.TEXTURE1),e.bindTexture(e.TEXTURE_2D,oe.blurTexture),e.activeTexture(e.TEXTURE2),e.bindTexture(e.TEXTURE_2D,se.blurTexture),e.activeTexture(e.TEXTURE3),e.bindTexture(e.TEXTURE_2D,De.fb.read.targetTexture),e.activeTexture(e.TEXTURE4),e.bindTexture(e.TEXTURE_2D,Fe.fb.read.targetTexture),e.activeTexture(e.TEXTURE5),e.bindTexture(e.TEXTURE_2D,ue.fb.read.targetTexture),qe.renderer.bindUniform("u_trailsTex",0),qe.renderer.bindUniform("u_particlemap",1),qe.renderer.bindUniform("u_dvmap",2),qe.renderer.bindUniform("u_sdfTex",3),qe.renderer.bindUniform("u_forceTex",4),qe.renderer.bindUniform("u_noiseTex",5),qe.renderer.bindUniform("u_time",.001*n),qe.renderer.bindUniform("u_resolution",[e.canvas.width,e.canvas.height]),qe.renderer.bindUniform("u_dpr",Z.dpr),qe.renderer.bindUniform("u_rf0",Z.rf0),qe.renderer.bindUniform("u_rf1",Z.rf1),qe.renderer.bindUniform("u_renderPer",r),qe.renderer.bindUniform("u_preloader",t),qe.renderer.bindUniform("u_preloaderOut",o),qe.renderer.bindUniform("u_colorMode",Z.colorMode),qe.renderer.bindUniform("u_paletteId",Z.paletteId),qe.renderer.bindUniform("u_cd0",Z.cd0),qe.renderer.bindUniform("u_cd1",Z.cd1),qe.renderer.bindUniform("u_cd2",Z.cd2),qe.renderer.bindUniform("u_colorId",Z.featurePalette),qe.renderer.bindUniform("u_screenMode",Z.screenMode),qe.renderer.bindUniform("u_shellType",Z.shellType),qe.renderer.bindUniform("u_lightDir0",Z.lightDir0),qe.renderer.bindUniform("u_lightDir1",Z.lightDir1),qe.renderer.bindUniform("u_saturation",Z.saturation),qe.renderer.bindUniform("u_mapId",Ye),qe.renderer.render(e.canvas.width,e.canvas.height)},je=(e,n)=>{let r=e.key;"m"==r?(Ye++,Ye%=5):"s"==r&&(e=>{const n=e.canvas.toDataURL("image/png",1).replace("image/png","image/octet-stream"),r=document.createElement("a");r.download=`${fxhash}_`+Z.interiorType+"_"+Z.exteriorType+".png",r.href=n,r.click()})(n)},Ke=document.createElement("canvas"),We=Ke.getContext("webgl2",{premultipliedAlpha:!0,alpha:!0,preserveDrawingBuffer:!0});let Ze=1,$e=1,Je=0,Qe=0,en=0,nn=0,rn=0;document.addEventListener("keydown",(function(e){let n=e.key;" "==n||("l"==n?(Z.dpr=2,an(),sn()):"h"==n&&(Z.dpr=4,an(),sn())),je(e,We)}),!1);const tn=We.getExtension("EXT_color_buffer_float");We.getExtension("OES_texture_float_linear"),We.getExtension("EXT_float_blend");const on=["iPad Simulator","iPhone Simulator","iPod Simulator","iPad","iPhone","iPod"].includes(navigator.platform)||navigator.userAgent.includes("Mac")&&"ontouchend"in document;(Z.hasFloat=!!tn)&&!on?(Z.fbType=We.FLOAT,Z.fbFormat=We.RGBA32F):(Z.fbType=We.HALF_FLOAT,Z.fbFormat=We.RGBA16F,Z.mapScale=mapSize/256*2*4),document.body.appendChild(Ke);const an=()=>{$e=1,ln=1,en=0,Ze=1},sn=()=>{const e=window.innerWidth,n=window.innerHeight;let r=Z.dpr;const t=1100*r,o=1500*r;let i,a,s=t/o;e/n>=1?(a=n,i=a*s):(a=e,i=a*s),Ke.width=t,Ke.height=o,console.log(`${fxhash}`)};var cn;window.addEventListener("resize",sn),cn=We,ue.renderer=s(cn,{vert:e($),frag:e("#version 300 es\nprecision highp float;\n#define GLSLIFY 1\n\nuniform vec2 u_resolution;\nuniform vec2 u_offset;\n\nout vec4 color;\n\n#define F4 0.309016994374947451\n#define PI 3.14159\n#define TAU 6.28318\n\nvec2 getScreenSpace() {\n  vec2 uv = (gl_FragCoord.xy - 0.5 * u_resolution.xy) / min(u_resolution.y, u_resolution.x);\n  \n  return uv;\n}\n\n// Description : Array and textureless GLSL 2D/3D/4D simplex\n//               noise functions.\n//      Author : Ian McEwan, Ashima Arts.\n//  Maintainer : ijm\n//     Lastmod : 20110822 (ijm)\n//     License : Copyright (C) 2011 Ashima Arts. All rights reserved.\n//               Distributed under the MIT License. See LICENSE file.\n//               https://github.com/ashima/webgl-noise\n//\n\nvec4 mod289(vec4 x) {\n  return x - floor(x * (1.0 / 289.0)) * 289.0;\n}\n\nfloat mod289(float x) {\n  return x - floor(x * (1.0 / 289.0)) * 289.0;\n}\n\nvec4 permute(vec4 x) {\n  return mod289(((x*34.0)+1.0)*x);\n}\n\nfloat permute(float x) {\n  return mod289(((x*34.0)+1.0)*x);\n}\n\nvec4 taylorInvSqrt(vec4 r) {\n  return 1.79284291400159 - 0.85373472095314 * r;\n}\n\nfloat taylorInvSqrt(float r) {\n  return 1.79284291400159 - 0.85373472095314 * r;\n}\n\nvec4 grad4(float j, vec4 ip) {\n  const vec4 ones = vec4(1.0, 1.0, 1.0, -1.0);\n  vec4 p,s;\n\n  p.xyz = floor( fract (vec3(j) * ip.xyz) * 7.0) * ip.z - 1.0;\n  p.w = 1.5 - dot(abs(p.xyz), ones.xyz);\n  s = vec4(lessThan(p, vec4(0.0)));\n  p.xyz = p.xyz + (s.xyz*2.0 - 1.0) * s.www;\n\n  return p;\n}\n\nfloat snoise(vec4 v) {\n  const vec4  C = vec4( 0.138196601125011,  // (5 - sqrt(5))/20  G4\n          0.276393202250021,  // 2 * G4\n          0.414589803375032,  // 3 * G4\n          -0.447213595499958); // -1 + 4 * G4\n\n  // First corner\n  vec4 i  = floor(v + dot(v, vec4(F4)) );\n  vec4 x0 = v -   i + dot(i, C.xxxx);\n\n  // Other corners\n\n  // Rank sorting originally contributed by Bill Licea-Kane, AMD (formerly ATI)\n  vec4 i0;\n  vec3 isX = step( x0.yzw, x0.xxx );\n  vec3 isYZ = step( x0.zww, x0.yyz );\n  //  i0.x = dot( isX, vec3( 1.0 ) );\n  i0.x = isX.x + isX.y + isX.z;\n  i0.yzw = 1.0 - isX;\n  //  i0.y += dot( isYZ.xy, vec2( 1.0 ) );\n  i0.y += isYZ.x + isYZ.y;\n  i0.zw += 1.0 - isYZ.xy;\n  i0.z += isYZ.z;\n  i0.w += 1.0 - isYZ.z;\n\n  // i0 now contains the unique values 0,1,2,3 in each channel\n  vec4 i3 = clamp( i0, 0.0, 1.0 );\n  vec4 i2 = clamp( i0-1.0, 0.0, 1.0 );\n  vec4 i1 = clamp( i0-2.0, 0.0, 1.0 );\n\n  //  x0 = x0 - 0.0 + 0.0 * C.xxxx\n  //  x1 = x0 - i1  + 1.0 * C.xxxx\n  //  x2 = x0 - i2  + 2.0 * C.xxxx\n  //  x3 = x0 - i3  + 3.0 * C.xxxx\n  //  x4 = x0 - 1.0 + 4.0 * C.xxxx\n  vec4 x1 = x0 - i1 + C.xxxx;\n  vec4 x2 = x0 - i2 + C.yyyy;\n  vec4 x3 = x0 - i3 + C.zzzz;\n  vec4 x4 = x0 + C.wwww;\n\n  // Permutations\n  i = mod289(i);\n  float j0 = permute( permute( permute( permute(i.w) + i.z) + i.y) + i.x);\n  vec4 j1 = permute( permute( permute( permute (\n                      i.w + vec4(i1.w, i2.w, i3.w, 1.0 ))\n                  + i.z + vec4(i1.z, i2.z, i3.z, 1.0 ))\n              + i.y + vec4(i1.y, i2.y, i3.y, 1.0 ))\n          + i.x + vec4(i1.x, i2.x, i3.x, 1.0 ));\n\n  // Gradients: 7x7x6 points over a cube, mapped onto a 4-cross polytope\n  // 7*7*6 = 294, which is close to the ring size 17*17 = 289.\n  vec4 ip = vec4(1.0/294.0, 1.0/49.0, 1.0/7.0, 0.0) ;\n\n  vec4 p0 = grad4(j0,   ip);\n  vec4 p1 = grad4(j1.x, ip);\n  vec4 p2 = grad4(j1.y, ip);\n  vec4 p3 = grad4(j1.z, ip);\n  vec4 p4 = grad4(j1.w, ip);\n\n  // Normalise gradients\n  vec4 norm = taylorInvSqrt(vec4(dot(p0,p0), dot(p1,p1), dot(p2, p2), dot(p3,p3)));\n  p0 *= norm.x;\n  p1 *= norm.y;\n  p2 *= norm.z;\n  p3 *= norm.w;\n  p4 *= taylorInvSqrt(dot(p4,p4));\n\n  // Mix contributions from the five corners\n  vec3 m0 = max(0.6 - vec3(dot(x0,x0), dot(x1,x1), dot(x2,x2)), 0.0);\n  vec2 m1 = max(0.6 - vec2(dot(x3,x3), dot(x4,x4)            ), 0.0);\n  m0 = m0 * m0;\n  m1 = m1 * m1;\n  return 49.0 * ( dot(m0*m0, vec3( dot( p0, x0 ), dot( p1, x1 ), dot( p2, x2 )))\n          + dot(m1*m1, vec2( dot( p3, x3 ), dot( p4, x4 ) ) ) ) ;\n\n}\nfloat surface( vec4 coord ) {\n    float n = 0.0;\n\n    n += 0.25 * abs( snoise( coord * 4.0 ) );\n    n += 0.5 * abs( snoise( coord * 8.0 ) );\n    n += 0.25 * abs( snoise( coord * 16.0 ) );\n    n += 0.125 * abs( snoise( coord * 32.0 ) );\n\n    return n;\n\n}\n\nvoid main() {\n    vec2 uv = getScreenSpace();\n    \n    const vec2 uvScale0 = vec2( 0.3 );\n    const vec2 uvScale1 = vec2( 1.0 );\n    const vec2 uvScale2 = vec2( 3.0 );\n    const vec2 uvScale3 = vec2( 7.0 );\n    vec2 st0 = uv * uvScale0;\n    vec2 st1 = uv * uvScale1;\n    vec2 st2 = uv * uvScale2;\n    vec2 st3 = uv * uvScale3;\n\n    // Tiling 4d noise based\n    const mat2 mat = mat2(-.0, -.99999, .99999, -.0);\n    \n    st0 *= TAU;\n    st1 *= TAU;\n    st2 *= TAU;\n    st3 *= TAU;\n\n    float multiplier = 1.0 / ( TAU );\n    vec4 ns0 = vec4(\n        cos( st0.x + u_offset.x ) * multiplier,\n        cos( st0.y + u_offset.y ) * multiplier,\n        sin( st0.x + u_offset.x ) * multiplier,\n        sin( st0.y + u_offset.y ) * multiplier );\n    ns0.zw *= mat;\n\n    vec4 ns1 = vec4(\n        cos( st1.x + u_offset.x ) * multiplier,\n        cos( st1.y + u_offset.y ) * multiplier,\n        sin( st1.x + u_offset.x ) * multiplier,\n        sin( st1.y + u_offset.y ) * multiplier );\n    ns1.zw *= mat;\n\n    vec4 ns2 = vec4(\n        cos( st2.x + u_offset.x ) * multiplier,\n        cos( st2.y + u_offset.y ) * multiplier,\n        sin( st2.x + u_offset.x ) * multiplier,\n        sin( st2.y + u_offset.y ) * multiplier );\n    ns2.zw *= mat;\n    // ns.xz *= mat;\n    vec4 ns3 = vec4(\n        cos( st3.x + u_offset.x ) * multiplier,\n        cos( st3.y + u_offset.y ) * multiplier,\n        sin( st3.x + u_offset.x ) * multiplier,\n        sin( st3.y + u_offset.y ) * multiplier );\n    ns3.zw *= mat;\n    \n    float n0 = surface( ns0 );\n    float n1 = surface( ns1 );\n    float n2 = surface( ns2 );\n    float n3 = surface( ns3 );\n\n    color = vec4( n0, n1, n2, n3 );\n}"),attributes:{position:{data:fe,size:2},uv:{data:de,size:2}},count:3}),ue.fb=c(cn,{width:Z.height,height:Z.height,type:Z.fbType,internalFormat:Z.fbFormat,tiling:cn.REPEAT}),(n=>{De.renderer=s(n,{vert:e($),frag:e("#version 300 es\nprecision highp float;\n#define GLSLIFY 1\n\nuniform vec2 u_resolution;\n\nuniform sampler2D u_obstaclesTex;\nuniform int u_obsRes;\nuniform float u_obsInvRes;\n\nout vec4 color;\n\n%%common%%\n\nvec2 getScreenSpace() {\n  vec2 uv = ( gl_FragCoord.xy - 0.5 * u_resolution.xy ) / min( u_resolution.y, u_resolution.x );\n  return uv;\n}\n\nvoid main() {\n  \tfloat myDist \t= 100000.0;\n    vec2 myGrad     = vec2( 0.0, 0.0 );\n    vec3 mySdg      = vec3( 100000.0, 0.0, 0.0 );  \n\tfloat myCenterDist = 100000.0;\n    float myRadius  = 100000.0;\n    float maxRadius = 1.0;\n\n\tfloat dres \t\t= u_obsInvRes * 0.5;\n  \tfor( int x=0; x<u_obsRes; x++ ){\n\t\tfloat xp = float( x ) * u_obsInvRes + dres;\n\t\tfor( int y=0; y<u_obsRes; y++ ){\n\t\t\tfloat yp \t\t= float( y ) * u_obsInvRes + dres;\n\n\t\t\tvec2 uv\t\t    = vec2( xp, yp );\n\t\t\tvec4 data \t\t= texture( u_obstaclesTex, uv, 0.0 );\n\t\t\tvec2 p \t\t    = data.xy;\n\t\t\tfloat r\t\t    = data.z; \n\t\t\tint id\t\t\t= int( data.w );\n\t\t\t\n\t\t\tvec2 dir \t\t= gl_FragCoord.xy - p;\n\t\t\tvec3 sdg        = getSdg( dir, r, id );\n            float dist\t\t= length( dir );\n\n            mySdg           = sdgSmoothMin( sdg, mySdg, 35.0 );\n            dist = mySdg.x;\n            //mySdg           = sdgMin( sdg, mySdg );\n            mySdg.yz        = normalize( mySdg.yz );\n\n            if( dist < 0.0 ){\n                if( r > maxRadius ){\n                    maxRadius = r;\n                }\n                //if( dist < myCenterDist ){\n                    myCenterDist = dist;\n                    myRadius = r;\n                //}\n            }\n\t\t}\n\t}\n    color = vec4( mySdg, maxRadius );\n}").replace("%%common%%",e("#define GLSLIFY 1\n#define PI 3.14159265359\n#define TAU 6.28318530718\n\nfloat sdCircle( in vec2 p, in float r ) \n{\n    return length(p)-r;\n}\n\nvec3 sdgCircle( in vec2 p, in float r ) \n{\n    float d = length(p);\n    return vec3( d-r, p/d );\n}\n\nfloat sdSegment( in vec2 p, in vec2 a, in vec2 b )\n{\n    vec2 pa = p-a, ba = b-a;\n    float h = clamp( dot(pa,ba)/dot(ba,ba), 0.0, 1.0 );\n    return length( pa - ba*h );\n}\n\nvec3 sdgSegment( in vec2 p, in vec2 a, in vec2 b, in float r )\n{\n    vec2 ba = b-a, pa = p-a;\n    float h = clamp( dot(pa,ba)/dot(ba,ba), 0.0, 1.0 );\n    vec2  q = pa-h*ba;\n    float d = length(q);\n    return vec3(d-r,q/d);\n}\n\nfloat sdBox( in vec2 p, in vec2 b )\n{\n    vec2 d = abs(p)-b;\n    return length(max(d,0.0)) + min(max(d.x,d.y),0.0);\n}\n\nvec3 sdgBox( in vec2 p, in vec2 b )\n{\n    vec2 w = abs(p)-b;\n    vec2 s = vec2(p.x<0.0?-1:1,p.y<0.0?-1:1);\n    float g = max(w.x,w.y);\n    vec2  q = max(w,0.0);\n    float l = length(q);\n    return vec3(   (g>0.0)?l  :g,\n                s*((g>0.0)?q/l:((w.x>w.y)?vec2(1,0):vec2(0,1))));\n}\n\nfloat sdHexagon( in vec2 p, in float r )\n{\n    const vec3 k = vec3(-0.866025404,0.5,0.577350269);\n    p = abs(p);\n    p -= 2.0*min(dot(k.xy,p),0.0)*k.xy;\n    p -= vec2(clamp(p.x, -k.z*r, k.z*r), r);\n    return length(p)*sign(p.y);\n}\n\nvec3 sdgHexagon( in vec2 p, in float r ) \n{\n    const vec3 k = vec3(-0.866025404,0.5,0.577350269);\n    vec2 s = sign(p); p = abs(p);\n    float w = dot(k.xy,p);    \n    p -= 2.0*min(w,0.0)*k.xy;\n    p -= vec2(clamp(p.x, -k.z*r, k.z*r), r);\n    float d = length(p)*sign(p.y);\n    vec2  g = (w<0.0) ? mat2(-k.y,-k.x,-k.x,k.y)*p : p;\n    return vec3( d, s*g/d );\n}\n\nfloat sdTriangleIsosceles( in vec2 p, in vec2 q )\n{\n    p.x = abs(p.x);\n    vec2 a = p - q*clamp( dot(p,q)/dot(q,q), 0.0, 1.0 );\n    vec2 b = p - q*vec2( clamp( p.x/q.x, 0.0, 1.0 ), 1.0 );\n    float s = -sign( q.y );\n    vec2 d = min( vec2( dot(a,a), s*(p.x*q.y-p.y*q.x) ),\n                  vec2( dot(b,b), s*(p.y-q.y)  ));\n    return -sqrt(d.x)*sign(d.y);\n}\n\nvec3 sdgTriangleIsosceles( in vec2 p, in vec2 q )\n{\n    float w = sign(p.x);\n    p.x = abs(p.x);\n    vec2 a = p - q*clamp( dot(p,q)/dot(q,q), 0.0, 1.0 );\n    vec2 b = p - q*vec2( clamp( p.x/q.x, 0.0, 1.0 ), 1.0 );\n    float k = sign(q.y);\n    float l1 = dot(a,a);\n    float l2 = dot(b,b);\n    float d = sqrt((l1<l2)?l1:l2);\n    vec2  g =      (l1<l2)? a: b;\n    float s = max( k*(p.x*q.y-p.y*q.x),k*(p.y-q.y)  );\n    return vec3(d,vec2(w*g.x,g.y)/d)*sign(s);\n}\n\nvec3 sdgCross( in vec2 p, in vec2 b ) \n{\n    vec2 s = sign(p); p = abs(p); \n    vec2  q = ((p.y>p.x)?p.yx:p.xy) - b;\n    float h = max( q.x, q.y );\n    vec2  o = max( (h<0.0)?vec2(b.y-b.x,0.0)-q:q, 0.0 );\n    float l = length(o);\n    vec3  r = (h<0.0 && -q.x<l)?vec3(-q.x,1.0,0.0):vec3(l,o/l);\n    return vec3( sign(h)*r.x, s*((p.y>p.x)?r.zy:r.yz) );\n}\n\nfloat sdRoundedX( in vec2 p, in float w, in float r )\n{\n    p = abs(p);\n    return length(p-min(p.x+p.y,w)*0.5) - r;\n}\n\nvec3 sdgMin( in vec3 a, in vec3 b )\n{\n    return (a.x<b.x)?a:b;\n}\n\nvec3 sdgSmoothMin( in vec3 a, in vec3 b, in float k )\n{\n    float h = max(k-abs(a.x-b.x),0.0);\n    float m = 0.25*h*h/k;\n    float n = 0.50*  h/k;\n    return vec3( min(a.x,  b.x) - m, \n                 mix(a.yz, b.yz, (a.x<b.x)?n:1.0-n) );\n}\n\nvec3 getSdg( vec2 p, float r, int id ){\n    vec3 v = vec3( 0.0, 0.0, 0.0 );\n    if( id == 0 ){\n        v = sdgCircle( p, r );\n    } else if( id == 1 ){\n        v = sdgHexagon( p, r );\n    } else if( id == 2 ){\n        v = sdgCross( p, vec2( r, r*0.35 ) );\n    } else if( id == 3 ){\n        v = sdgSegment( p, vec2( -r*1.0, 0.0 ), vec2( r*1.0, 0.0 ), r * 0.2 );\n    } else {\n        v = sdgSegment( p, vec2( 0.0, -r*3.0 ), vec2( 0.0, r*3.0 ), r * 0.25 );\n    }\n    return v;\n}\n\n")),attributes:{position:{data:he,size:2},uv:{data:Te,size:2}},count:3}),De.fb=c(n,{width:Z.height,height:Z.height,type:Z.fbType,internalFormat:Z.fbFormat,tiling:n.REPEAT}),De.obstacleTexture=n.createTexture(),n.bindTexture(n.TEXTURE_2D,De.obstacleTexture),n.texImage2D(n.TEXTURE_2D,0,n.RGBA32F,Z.obstacleSize,Z.obstacleSize,0,n.RGBA,n.FLOAT,ge.obstacleData),n.texParameteri(n.TEXTURE_2D,n.TEXTURE_MIN_FILTER,n.NEAREST),n.texParameteri(n.TEXTURE_2D,n.TEXTURE_MAG_FILTER,n.NEAREST),n.texParameteri(n.TEXTURE_2D,n.TEXTURE_WRAP_S,n.CLAMP_TO_EDGE),n.texParameteri(n.TEXTURE_2D,n.TEXTURE_WRAP_T,n.CLAMP_TO_EDGE)})(We),(n=>{Fe.renderer=s(n,{vert:e($),frag:e("#version 300 es\nprecision highp float;\n#define GLSLIFY 1\n\nuniform vec2 u_canvas_res;\nuniform int u_numForces;\nuniform float u_rf0;\nuniform float u_rf1;\nout vec4 color;\n\n#define PI 3.14159265359\n#define TAU 6.28318530718\n\n// Hash without Sine\n// MIT License...\n// Copyright (c)2014 David Hoskins.\nfloat hash11( float p )\n{\n    p = fract(p * .1031);\n    p *= p + 33.33;\n    p *= p + p;\n    return fract(p);\n}\n\nfloat hash12( vec2 p )\n{\n\tvec3 p3  = fract(vec3(p.xyx) * .1031);\n    p3 += dot(p3, p3.yzx + 33.33);\n    return fract((p3.x + p3.y) * p3.z);\n}\n\nvec4 getForceData( vec2 p ){\n    float x = hash12( p + vec2( 15.0, 11.0 ) * u_rf0 ) * u_canvas_res.x;\n    float y = hash12( p + vec2( 22.0, 13.5 ) * u_rf0 ) * u_canvas_res.y;\n    float rf0 = hash12( p + vec2( 15.0, 18.5 ) * u_rf0 ) * 4.0;\n    float rf1 = ( hash12( p + vec2( 33.0, 55.5 ) * u_rf0 ) - 0.5 ) * 28.0;\n    return vec4( x, y, rf0, rf1 );\n}\n\nvoid main() {\n  \tfloat myDist \t= 10000.0;\n\tvec2 myVel \t\t= vec2( 0.0, 0.0 );\n\tfloat myF       = 0.0;\n\tvec2 p0 \t\t= gl_FragCoord.xy;\n\n    for( int i=0; i<u_numForces; i++ ){\n        float u         = hash12( vec2( float( i ) + 0.0, float( i ) + 5.0 ) );\n        float v         = hash12( vec2( float( i ) + 17.7, float( i ) + 3.5 ) );\n        vec4 forceData  = getForceData( vec2( u, v ) );\n        vec2 p1         = forceData.xy;\n        float r         = forceData.z;\n        float f         = forceData.w;\n\n        vec2 dir \t\t= p0 - p1;\n        float dist \t\t= distance( p0, p1 );\n        vec2 dirNorm    = normalize( dir + vec2( 0.001, 0.001 ) );\n        float distPer\t= clamp( ( 1500.0 - dist ) / 1500.0, 0.0, 1.0 );\n        myVel += vec2( dirNorm.y, -dirNorm.x ) * f * distPer;\n\n        if( dist < myDist ){\n            myDist = dist;\n            myF     = f;\n        }\n    }\n    \n  \tcolor = vec4( myVel.x, myVel.y, myDist, myF );\n}"),attributes:{position:{data:ze,size:2},uv:{data:we,size:2}},count:3}),Fe.fb=c(n,{width:Z.height,height:Z.height,type:Z.fbType,internalFormat:Z.fbFormat,tiling:n.REPEAT})})(We),Ae(We),ie(We),ce(We),Ge(We),ke(We);let ln=!0;const fn=e=>{ln&&(Ae(e),ie(e),ce(e),Ge(e),ke(e),ve(e),Ee(e),Re(e),ln=!1)},dn=e=>{if(requestAnimationFrame(dn),1==Ze){let e=150;rn=Math.max(Math.min(parseFloat(en-90)/parseFloat(e),1),0)}else rn=0;let n=Math.min(en/90,1),r=0;en>90&&(r=Math.min((en-90)/30,1)),Qe=e-Je,fn(We),rn<.999&&(Be(We,e,rn),ae(We),le(We),Ve(We,rn,Ze)),He(We,e,rn,n,r),Je=e,nn+=.03333333333333333,en++,$e=0};requestAnimationFrame(dn),sn();